# PROGRESS — Mission v2 (Zero-Fulfillment-Website-Maschine)

Branch: `refactor/mission-v2` · Basis-Commit: `55a67fa` (wip: stand vor mission-v2)

## Phasen-Status

| Phase | Inhalt | Status |
|-------|--------|--------|
| 0 | Git-Sicherheitsnetz, PROGRESS.md, WARTELISTE.md | ✅ erledigt (2026-07-15) |
| A | Inventur & Audit → AUDIT.md (KEEP/REFACTOR/KILL) | ✅ erledigt (2026-07-15) |
| B | Aufräumen (/_legacy/), Landing als `(marketing)`, Lead-Form → leads-Tabelle, Host-Routing-Middleware | ✅ erledigt (2026-07-15) |
| C | Template-Library + Seeding (4 Branchen × 2 Stile) | ✅ erledigt (2026-07-15) |
| D | Generierungs-Pipeline v2 (Firecrawl → Places → manueller Fallback) | ✅ erledigt (2026-07-15) |
| E | Checkout, Verträge (24/24/3), Provisioning, Dunning, Kündigung | ✅ erledigt (2026-07-15) |
| F | Portal: Wizard, Chat-Editor (nur strukturierte Ops), Pläne, Upsell-Leiter | ✅ erledigt (2026-07-15) |
| G | Domains & Upsell-Fulfillment | ✅ erledigt (2026-07-15) |
| H | CI-Gates: Golden-Set, Lighthouse-CI, Playwright E2E, /admin/kennzahlen | ✅ erledigt (2026-07-15) |

## Sitzungs-Log

### 2026-07-15 — Session 1
- §0.1: WIP-Commit `55a67fa` auf main, Branch `refactor/mission-v2` erstellt
- §0.2: PROGRESS.md + WARTELISTE.md angelegt
- Vorarbeit (vor Masterprompt, bereits im WIP-Commit enthalten):
  - Demo-System komplett (Migration 013, scrape-prospect, generate-demo, /admin/demos, /demo/[token])
  - Stripe-Closing-Flow komplett (Migration 014, lib/stripe, payment-link-Route, Webhook mit Auto-Provisioning, /willkommen)
  - Landing-Repo `FelixZoepp/Webseiten-wvd` nach `/tmp/webseiten-wvd` geklont + Kompatibilitätsanalyse (Next 16→14, TW4→TW3, src/-Pfade) — Integration folgt in Phase B
- Phase A: Voll-Inventur (53+ Routen, 32 lib-Dateien, 15 Migrationen, Env-Vars, Cron) → AUDIT.md mit KEEP / 12× REFACTOR (R1–R12) / 8× KILL (K1–K8). Typecheck + Lint grün.
- Wichtigste Audit-Erkenntnisse: `lib/payment.ts` = No-Op-Stub (einziger Importer: upsell-orchestrator → R5), `lib/defaults.ts` tot, DocuSign/SEPA/Qonto/EasyBill → _legacy in Phase B, Cloudflare-Tokens im Klartext (R3), `template-renderer.ts` hat noch 4 Importer (K8 erst Phase C)

- **Phase B komplett** (Commits `17322b9`, `e73f09b`, `26480cb`):
  - B1: DocuSign/SEPA/IBAN-Closing-Pfad → `_legacy/` (siehe `_legacy/README.md`); tote Flags entfernt; Admin-CTAs → Demo-Maschine. Audit-Korrektur: `lib/payment.ts` bleibt (aktives rechnungs_posten-Ledger, Ablösung in Phase E)
  - B2: Migration `015_leads.sql` (leads mit UTM-Attribution, RLS admin-only)
  - B3: Landing aus Webseiten-wvd als `(marketing)`-Route-Group; CSS auf `.marketing-root` gescoped; Fonts self-hosted (next/font); `/api/public/lead` mit Honeypot+Rate-Limit+Resend; CTA-Formular war Attrappe → verdrahtet; Preise 89→99€ angeglichen; Nav mit /login
  - B4: Host-Routing-Middleware (env-gesteuert, ohne Env-Vars no-op)
  - B5: Smoke-Tests grün (/, /entwurf, /blog, /login = 200; Lead-Validierung; Honeypot; 0 Font-CDN-Refs), Build+Lint+Typecheck grün

- **Phase C komplett** (Commit `93aaafb`):
  - C2: Migration `016_template_library.sql` — section_library, library_pages, stock_assets (RLS admin-only, Rendering läuft serverseitig)
  - C3: Seeding via `scripts/seed-library.ts` (idempotent, upsert on key): 40 Sektions-Bausteine (10-Sektionen-Skeleton §2 × Handwerk/Gastronomie/Friseur/Gesundheit) + 8 Startseiten-Kompositionen (Stil `klar` = Defaults, `warm` = Tonalitäts-Overrides auf Hero/CTA) + 15 Stock-Assets (Unsplash-Platzhalter aus Bestand)
  - Floskel-Blacklist `lib/floskel-blacklist.ts` (§2 Quality-Gate) — geprüft beim Seeding (`--check`), ab Phase D auch in der Pipeline
  - C4/K8-Korrektur: `renderTemplate` ≠ Drop-in für `renderPremiumTemplate` (inkompatible Datenformen, Legacy-Configs in config_versions) → Ablösung verschoben auf Phase F/G, dokumentiert in AUDIT.md
  - C5: `npx tsx scripts/seed-library.ts --check` grün (Skeleton vollständig, Referenzen auflösbar, Floskel-frei); Build+Lint+Typecheck grün

- **Phase D komplett** (Commit `3213b72`):
  - D2: `lib/pipeline/prospect-data.ts` — Datenkette Firecrawl (`FIRECRAWL_API_KEY`) → eigener Scraper → Google Places (`GOOGLE_PLACES_API_KEY`: Adresse, Telefon, Öffnungszeiten, echte Bewertungen ≥4★) → manuelle Admin-Eingaben. Alles env-gesteuert, ohne Keys wie v1
  - D3: `lib/pipeline/generate-library-content.ts` — Claude (`claude-sonnet-4-6`) füllt content_schema-Felder; Floskel-Gate mit 1 Retry, danach Fallback auf token-ersetzte Defaults (Demo entsteht IMMER, auch ohne ANTHROPIC_API_KEY); echte Google-Bewertungen ersetzen generierte Stimmen. `lib/library/load.ts` = DB-first-Loader mit Seed-Fallback (läuft vor Migration 016)
  - D4: `lib/library/render.ts` — Zero-JS-Renderer (§2): System-Fonts, ein Inline-`<style>`, `<details>`-FAQ, durchgängiges Escaping, Design-Tokens je Stil (klar/warm) + Branchen-Akzentfarben, ~10 KB HTML
  - D5: Migration `017_demos_engine.sql` (additiv, `demos.engine` default 'premium'); POST/PATCH `/api/admin/demos` mit Library-Engine (Branche+Stil → pageKey, template_id = pageKey); `/demo/[token]` rendert nach `config.engine` (migrationsunabhängig); Admin-UI: Library-Optionen (4×2) + Ort-Feld
  - D6: Offline-Smoke-Test grün (8 Kompositionen, Escaping inkl. `<script>`-Injection in Bewertungen, keine Tokens/CDNs/Floskeln, alle 10 Sektionen); Build+Lint+Typecheck grün

- **Phase E komplett** (Commit `7b1d67c`):
  - E2: Migration `018_contracts.sql` — `contracts` (Konditionen 24/24/3 pro Zeile konfigurierbar, Mahnwesen-Felder, Stripe-Verknüpfung) + `manual_tasks` (Zero-Fulfillment-Ausnahmen, Status OFFEN/ERLEDIGT/VERWORFEN) + `sites.gesperrt`; RLS admin-only
  - E3: `lib/contracts.ts` — monatsende-sichere UTC-Datumslogik (`addiereMonate`, `wirksamesKuendigungsdatum` mit Mehrfach-Verlängerung + Endlosschleifen-Schutz), `createManualTask` (best effort, bricht nie den aufrufenden Flow). Stripe-Webhook zum Multi-Event-Handler umgebaut: `checkout.session.completed` provisioniert wie bisher UND legt Vertrag an (idempotent über stripe_subscription_id; Vertragsfehler → manual_task PROVISIONING_LUECKE + 200, KEIN 500 — Demo ist schon CONVERTED, Retry würde doppelt provisionieren)
  - E4: Dunning-Leiter über `invoice.payment_failed` (Stufe 1 Erinnerung → 2 Mahnung → 3 Site-Sperre + manual_task DUNNING_ESKALIERT), `invoice.paid` setzt Mahnstufe 0 + entsperrt; `customer.subscription.updated` (cancel_at_period_end) → GEKUENDIGT mit 24/24/3-Fristberechnung; `customer.subscription.deleted` → BEENDET + Sperre + Restforderungs-Task. `sendDunningEmail` (3 Stufen) in lib/email.ts. Stripe-API-Hinweis: `invoice.subscription` existiert in SDK 22 nicht mehr → `invoice.parent?.subscription_details?.subscription`
  - E5: `/admin/vertraege` (Verträge + offene manuelle Aufgaben, Erledigen/Verwerfen, Kündigen-Aktion), GET `/api/admin/contracts`, PATCH `/api/admin/manual-tasks/[taskId]`, POST `/api/admin/contracts/[id]/kuendigen` (Fristberechnung + best-effort Stripe `cancel_at`, Fehler → manual_task); Nav-Link unter Abrechnung
  - E6: Offline-Smoke `/tmp/smoke-contracts.ts` — 14 Checks grün (Schaltjahr/Monatsende, Kündigung am Fristtag/1 Tag zu spät/nach Laufzeitende, 12/12/1-Sonderkonditionen, Endlosschleifen-Schutz); Build+Lint+Typecheck grün
  - Bewusst offen gelassen: `lib/payment.ts`-Ledger (rechnungs_posten) läuft parallel weiter — Ablösung erst wenn Upsell-Flow in Phase G auf contracts umgestellt ist (AUDIT R5)

- **Phase F komplett**:
  - F2: Migration `019_portal.sql` — `upsell_orders` (Status OFFEN→BEZAHLT→PROVISIONIERT, Stripe-Verknüpfung, contract_id) + `sites.noindex/fertiggestellt_am/wizard_status/pflichtangaben`; RLS: Kunden SELECT eigene, Schreiben nur Admin/Service-Role. Versionierung/Rollback läuft weiter über `config_versions` (001) — kein neues site_versions
  - F3: `config/plans.ts` (Feature-Wahrheit §10.3, Preise bleiben in Stripe; starter/business/growth=Premium) + `config/upsells.ts` (§10.5-Katalog: SEO-Unterseiten-Abo 49€/M, Stadtteil-Seiten 299€ einmalig, Bewertungs-System 149€+19€/M, Konkurrenz-Radar 29€/M, Saison-Kampagnen 39€/M, GBP-Einrichtung 199€ va_manual; je 3 Nutzen-Punkte, eigene Laufzeiten, `NERV_SCHUTZ_TAGE=60`, `KICKOFF_MODE`, verworfene Produkte dokumentiert)
  - F4: Fertigstellungs-Wizard §10.1 — `lib/rechtstexte.ts` (Impressum §5 DDG/§18 MStV + Datenschutzerklärung aus Pflichtangaben-Formular), `lib/wizard.ts` (5 Schritte, nur Pflichtangaben+Fakten sind Pflicht), `lib/qa.ts` (Auto-QA: Platzhalter/Lorem/alt/Meta/Formular/Größe), Routen `/api/sites/[siteId]/wizard` + `/fertigstellen` (QA-Fail → Hinweise, blockiert nie den Rest; Erfolg → `noindex=false` + config_versions-Eintrag), `components/fertigstellungs-wizard.tsx` + Banner im Dashboard. Domain-Schritt = Stub (Cloudflare-Connect in Phase G)
  - F5: Chat-Editor NUR strukturierte Ops §10.2 — `lib/editor-ops.ts`: 6 Op-Typen (update_text, swap_image_from_pool_or_upload, set_theme_preset, add_section_from_library, reorder, toggle) als Zod-DiscriminatedUnion, max 20 Ops, Pfad-Schema blockt `__proto__`; Leitplanken: Telefon/Logo/Rechtstexte gesperrt, Farben NUR über 6 Presets, Hero/Kontakt/CTA weder löschbar noch ausblendbar, Hero bleibt erste Sektion, Bild-Hosts allowlisted, Längenlimits Headline/CTA; applyPatch = All-or-Nothing auf Kopie → `draft_config` (live erst über Veröffentlichen-Klick, Rollback über bestehende config_versions-Route). Claude liefert `<patch_ops>`, Server validiert; Rate-Limit 50 Nachrichten/Tag/Kunde (429)
  - F6: Upsell-/Upgrade-Kauf §10.4 — `lib/upsell-kauf.ts` (gemeinsamer Kaufweg: Order OFFEN → Stripe-Checkout mit `metadata.product_key/order_id`; Doppelbuchungs-Schutz über activated_upsells; Plan-Upgrade als `plan-upgrade:<tier>`, Downgrades abgewiesen), `lib/supabase/admin.ts` (Service-Role-Client), Kaufweg 1: POST `/api/admin/upsell-payment-link` (requireAdmin), Kaufweg 2: POST `/api/sites/[siteId]/upsell-checkout` + Portal-Kacheln `/dashboard/[siteId]/erweiterungen` (aktive Module markiert, Nerv-Schutz sortiert Abgelehnte ans Ende, Preise transparent). Webhook-Branch `handleUpsellCheckout`: BEZAHLT → eigener Vertrag je Buchung (paket=`upsell:<key>`, Konditionen aus config) → auto: activated_upsells-Upsert + PROVISIONIERT; va_manual: manual_task; Plan-Upgrade: customers/sites.package + Hauptvertrag angehoben + manual_task „altes Stripe-Abo beenden" (Proration-Pragmatismus); alle Lücken → manual_task PROVISIONING_LUECKE + 200 (kein Doppel-Provisioning durch Stripe-Retry)
  - F7/DoD: `scripts/smoke-editor-ops.ts` — 19 Checks grün (invalider Patch/rohes HTML abgewiesen, All-or-Nothing, gesperrte Pfade, Bild-Host-Allowlist, Theme nur Presets, Hero-Schutz bei Toggle+Reorder, >20 Ops); Rollback = bestehende `/api/sites/[siteId]/rollback` + config_versions (funktionsfähig); Build+Lint+Typecheck grün. Upgrade-Kauf-E2E im Stripe-Testmode braucht Migration 019 + Webhook (WARTELISTE)

- **Phase G komplett**:
  - G2: Auslieferung — `sites.gesperrt` → Sperr-Seite (410), `noindex` → Meta-Tag im Renderer; Wizard-Rechtstexte (`draft_config.rechtstexte`) werden als Impressum-/Datenschutz-Seiten ausgespielt
  - G3: Domains — Wunsch-Domain-Flow aus Wizard-Schritt 4 mit Mock-Registrar (`REGISTRAR_PROVIDER=mock`, `lib/registrar.ts`), DNS-Recheck, Portal-UI `/dashboard/[siteId]/domain`
  - G4: SEO-Plan-Automation (Upsell #1) — Cron `/api/cron/seo-plan` (monatlich, `0 6 1 * *`): 1 Keyword-Landingpage aus der Library via Slot-Pipeline, 1-Klick-Freigabe im Portal (`/dashboard/[siteId]/seo`, Sidebar-Nav), technischer SEO-Check, DataForSEO-Stub (Keys auf WARTELISTE)
  - G5: GBP-Ersteinrichtung (Upsell #2, va_manual) — Kauf erzeugt `gbp_setups`-Zeile (Migration 020) + verknüpfte manual_task (`createManualTask` gibt jetzt Task-ID zurück); `/admin/worklist` (GBP-Status-Flow OFFEN→IN_ARBEIT→ZUGRIFF_ERTEILT→FERTIG als Pill-Kette + offene Aufgaben); FERTIG provisioniert die Order und erledigt die Task automatisch
  - G6: Google Ads Starter (Upsell #3, 99 €/M Platzhalter → WARTELISTE) — `lib/ads-starter.ts`: deterministischer Kampagnen-Generator (Search + PMax, Test-Modus, Google-Zeichenlimits 30/90); Grundsatz „Werbebudget IMMER direkt Kunde↔Google" im Entwurf verankert; Webhook legt `ads_kampagnen` an + manual_task „MCC-Einladung"; Admin-APIs `/api/admin/ads` (POST = DoD-testbar ohne Stripe) + Status-PATCH; Wochen-Check-Cron `/api/cron/ads-check` (montags: Ziel-URL-Erreichbarkeit, Entwurfs-Vollständigkeit, Leistungsdaten-Stub bis Ads-API-Zugang)
  - G7: Portal-Rest — POST `/api/sites/[siteId]/billing-portal` (Stripe Customer Portal via `customers.stripe_customer_id`) + Button in `/rechnungen`; alte `/upgrade`-Seite → Redirect auf `/erweiterungen`, `components/upgrade-checkout.tsx` (Fake-Katalog) → `_legacy/components/`; Sidebar + Editor-Gate zeigen auf `/erweiterungen`; Admin-Zahlungslink-Panel im Upsells-Tab der Kundendetailseite (nutzt POST `/api/admin/upsell-payment-link`, Copy-Button)
  - G8/DoD: Domain-Neuregistrierung läuft mit Mock-Registrar durch; SEO-Cron erzeugt freigebbare Landingpage; GBP-Flow bis „Zugriff erteilt" über `/admin/worklist` testbar; Ads-Setup erzeugt Kampagnen-Entwurf im Test-Modus (POST `/api/admin/ads`). Build+Lint+Typecheck grün
  - Bewusst offen: kein async Re-Deploy-Queue (serverless ohne Queue-Infra — Re-Deploys laufen synchron im Request); AUDIT R5 (`lib/payment.ts`-Ledger) bleibt, weil der alte Admin-Aktivierungs-Flow (`/api/admin/customers/.../upsells/...` + Upsells-Tab) noch darauf läuft → Ablösung in Phase H

- **Phase H komplett**:
  - H2: Golden-Set §12 — `test/golden_set.json` (10 Firmen-Slots: 5 website / 3 google / 2 nichts, aktuell Platzhalter → WARTELISTE) + `scripts/ci-golden-set.ts` (`npm run ci:golden-set`): komplett offline (API-Keys werden im Prozess gelöscht, Supabase-Stub erzwingt Seed-Fallback), prüft je Firma Floskel-Blacklist, Hero-Block, Stadt im Hero, kein `<script>`/keine Fremd-CDNs — 82 Checks grün
  - H3: Lighthouse-CI §2.1 — `scripts/ci-lighthouse-render.ts` rendert 11 Seiten offline nach `.lighthouse/dist/` (8 Kompositionen 4 Branchen × 2 Stile mit festen Beispiel-Firmen + 3 Golden-Demos je Kategorie) mit hartem Fremd-CDN-Check (throw = Build rot); `lighthouserc.json` (`staticDistDir`, `maxAutodiscoverUrls: 0` — LHCI-Default prüft sonst nur 5 URLs!, Mobile-Emulation = LHCI-Default) mit Assertions Perf ≥0.9, SEO ≥0.95, A11y ≥0.9, Script ≤30 KB; `npm run ci:lighthouse`. Lokaler Lauf: alle 11 Seiten grün (Perf 0.92–0.99, SEO 1.0, A11y 0.91–1.0, 0 KB JS)
  - H4: Playwright-E2E — `e2e/journey.spec.ts` + `playwright.config.ts` (`npm run test:e2e`): kompletter Durchlauf Landing-Form (UTM in leads) → Admin → Library-Demo (Offline-Defaults) → `/demo/[token]` → Stripe-Checkout-Session (echter Testmode-Call) → Webhook-Simulation mit echt signierter Payload (`webhooks.generateTestHeaderString`; Handler macht keine Stripe-Nachfragen → kein CLI-Forwarding nötig) → Kunde+Site+24/24/3-Vertrag+CONVERTED → Kunden-Login (Passwort via Admin-API) → Wizard (Pflichtangaben, Fakten, Domain-Neuregistrierung Mock-Registrar, SEO-Upsell-Kauf inkl. zweiter Webhook-Sim → `upsell:seo-unterseiten-abo`-Vertrag) → Fertigstellen (Auto-QA) → Chat-Edit (nur mit ANTHROPIC_API_KEY, sonst Step-Skip) → Publish → Rollback (config_versions) → Kickoff-Touchpoint (GBP-Angebot in `/erweiterungen`). Env-gated über `E2E_ENABLED=1` + Supabase/Stripe-TEST-Keys (fehlen → sauberer Skip ohne Serverstart); Stop-Condition: Lauf gegen `sk_live_` wird hart verweigert; afterAll räumt Testdaten auf. Echter Lauf braucht Stripe-Testkeys + migrierte (Test-)Supabase → WARTELISTE
  - H5: Monitoring §12 — `lib/monitoring.ts` (Slack #errors/#money via Webhooks, Log-Stub ohne Env) + `lib/nutzung.ts` (Kosten-Events in `nutzungs_events`, Migration 021) + `GENERATION_KILL_SWITCH` (sperrt Demo-Generierung + Crons) + Kosten-Summary-Cron 6:30 (`/api/cron/kosten-summary`, vercel.json)
  - H6: `/admin/kennzahlen` — Funnel je UTM-Quelle (Leads → Demos → Käufe), MRR/ARPU/Upsell-Quote/Churn; MRR-Quelle der Wahrheit = `contracts` (Hauptverträge + `upsell:`-Verträge getrennt), `activated_upsells` zählt nur für Alt-Modul-IDs (sonst Dreifachzählung mit upsell_orders/Webhook-Verträgen); Nav-Link im Admin-Layout
  - H7 (AUDIT R5 erledigt): alter Upsell-Aktivierungs-Flow abgelöst — `lib/upsell-orchestrator.ts`, `lib/payment.ts` (rechnungs_posten-Schreibweg), `lib/upsell-emails.ts`, `/api/admin/customers/[customerId]/upsells/[upsellId]/*` → `_legacy/` (README-Tabelle ergänzt); Katalog-GET-Route neu auf upsell_orders-Kaufstatus; Upsells-Tab der Kundendetailseite = Zahlungslink-Panel + 7-Produkte-Katalog (Kauf-Status, keine manuelle Aktivierung mehr) + Alt-Bestand read-only; rechnungs_posten-Ansichten bleiben lesend für Altdaten
  - H8/DoD: Build + Lint + Typecheck + Golden-Set (82 Checks) + Lighthouse (11×) grün; `.github/workflows/ci.yml` (Lint, tsc, Golden-Set, Lighthouse; greift erst nach Git-Push → WARTELISTE); Playwright-Skip-Verhalten ohne Envs verifiziert

### 2026-07-15 — Session 2: Zyklus v2 (neuer Masterprompt + Branchen-Fabrik)
- Vorarbeit: `flagship_reinigung.html` + `flagship_restaurant.html` offline-fähig gemacht — 17 externe Bilder (Cloudfront + Panevino) nach `./assets/` als WebP, Panevino-Originale mit `original_`-Präfix (Quarantäne-Regel), Offline-Render-Check grün (Commit `8675c05`)
- §0: Git-Sicherheitsnetz — Branch `refactor/mission-v2` aktiv, neue Prompt-Dokumente committed (`030c290`), Arbeitsverzeichnis sauber
- **Phase A v2:** Neu-Audit gegen Masterprompt v2 + `BRANCHEN_FABRIK_PROMPT.md` → `AUDIT.md` komplett neu geschrieben (v1 in Git-Historie). Kernänderungen ggü. v1: Alt-Templates (`lib/templates/*`, 39 Renderer + Legacy-Renderer) von KEEP auf **KILL mit Übergangsbedingung** (Fremd-CDNs in ≥10 Templates, Schema-Diskrepanz; erst Ideen → LEGACY_NOTIZEN.md, F2-Reproduktion, Bestands-Migration); Library-Seed = REFACTOR (Flagship-Niveau fehlt); `stock_assets` → `asset_bank`; Alt-Pipeline v1 (Fireflies) → KILL nach Umhängen; Watchlist doppelter Wahrheiten (3 Renderer, packages.ts/plans.ts); Fehlt-Liste = F1–F5. Typecheck + Lint grün (0 Errors, nur Alt-Warnings)
- **STOPP für Review** (User-Auftrag): nächster Schritt nach Freigabe → Phase B v2 (LEGACY_NOTIZEN.md, Alt-Pipeline umhängen) bzw. F1 (Higgsfield-Motor) laut Branchen-Fabrik
- **F1 komplett — Higgsfield-Motor (BF §2 + §6, User: „starte mit F1"):**
  - Migration 022 (`asset_bank` nach Masterprompt-§6-Schema + `kosten_cent`/`gen_job_id`/`varianten`, quelle inkl. `demo_generiert`/`prospect_quarantaene`/`ai_mock`, RLS admin-only, Storage-Bucket `asset-bank` public; `nutzungs_events` additiv: Spalte `kosten_cent` + CHECK um `asset_generierung` erweitert) — auf Prod ausgeführt (Supabase MCP)
  - `lib/assets/higgsfield.ts`: `AssetProvider`-Interface exakt nach BF §2.1; HiggsfieldProvider (Endpoints per ENV konfigurierbar — unverifiziert bis echter Key, WARTELISTE), FalProvider (queue.fal.run), MockProvider (ohne Keys: sharp-Platzhalter als data-URL, Edit degradiert dasselbe Bild → makePair beweisbar gleiche Szene); Poll-Timeout 120 s
  - `lib/assets/pipeline.ts`: `generiereAsset()` — Kill-Switch → Tages-Budget (`ASSET_BUDGET_TAG_CENT`, Default 500 ct) → Provider-Kette (Higgsfield→fal→Mock) → Sofort-Download (Provider-URLs nie im Kundenoutput) → sharp WebP q82 + AVIF q55 in srcset-Stufen 480/960/1600 → Bucket-Upload → asset_bank-Insert (gen_prompt/gen_seed/gen_job_id/kosten_cent/varianten) → Kosten-Log via `erfasseNutzung('asset_generierung', {kostenCent})`; `makePair()` — IMMER Ziel (nachher) zuerst, Degradation (vorher) via referenceJobId beim SELBEN Provider, gemeinsame pair_id
  - CLI `npm run asset:paar` (`scripts/asset-paar.ts`, lädt .env.local selbst); sharp ^0.35.3 als Dependency; .env.local.example + WARTELISTE (neuer Block „Branchen-Fabrik F1")
  - **DoD-Beweis:** Mock-Lauf gegen Prod erzeugt Paar `51446303-…` — beide Bilder als WebP+AVIF (je 3 Größen) im Bucket `asset-bank`, beide asset_bank-Zeilen mit Prompt/Seed/Kosten/pair_id/quality_status=draft (SQL-verifiziert). Typecheck + Lint grün
- **Änderungsauftrag: Laufzeit-Standard 24/12/3 (statt 24/24/3) umgesetzt:**
  - `config/vertraege.ts` (NEU) = einzige Quelle: `HAUPTPRODUKT_KONDITIONEN` (24/12/3) + `konditionenKurz()` + `vertragsKonditionenText()` (Soll-Wortlaut des Auftrags; akzeptiert auch Vertragszeilen-Shape für Bestandsverträge)
  - `lib/contracts.ts`: STANDARD_KONDITIONEN mappt jetzt aus der Config (kein Hardcode mehr); **Datums-Konvention präzisiert:** `ende` = LETZTER Laufzeittag (`vertragsende('2026-08-01', 24)` → `2028-07-31`, vorher wäre 2028-08-01 herausgekommen — hätte die Referenztabelle des Auftrags verfehlt). `wirksamesKuendigungsdatum()` liest unverändert IMMER die Vertragszeile, nie die Config
  - Migration 023 (additiv, auf Prod ausgeführt via Supabase MCP): `contracts.verlaengerung_monate` DEFAULT 24 → 12; Bestandszeilen unangetastet (Tabelle auf Prod ohnehin leer)
  - `lib/stripe.ts` Checkout: `custom_text.submit.message` = Konditionen-Wortlaut aus Config (immer aktiv); AGB-Consent-Checkbox (`consent_collection` + `terms_of_service_acceptance`) gated hinter `STRIPE_TOS_CONSENT=1`, weil Stripe ohne hinterlegte AGB-URL die Session ablehnt → WARTELISTE
  - Kundensichtbare/Admin-Texte aus Config: Admin-Sidebar + Verträge-Topbar zeigen `Verträge (${konditionenKurz()})`; Webhook-Kündigungs-Task rechnet Beschreibung aus den Werten der Vertragszeile; `lib/pdf/angebot.ts` (Legacy, unreferenziert) Frist-Satz auf Config umgestellt statt „3 Monaten"-Hardcode
  - Tests: `npm run test:kuendigung` (`scripts/test-kuendigung.ts`) — alle 3 Referenz-Szenarien grün (15.03.2027→31.07.2028, 30.04.2028→31.07.2028, 15.05.2028→31.07.2029) inkl. Monatsende-Clamping-Check (31.07.−3M→30.04.); `e2e/journey.spec.ts` prüft zusätzlich `verlaengerung_monate === 12`
  - Idempotenz-Vermerke: kein Laufzeitende-Vorroll-Cron existiert (Crons: ads-check, briefings, kosten-summary, seo-plan) und keine Kündigungsbestätigungs-Mail → dort nichts zu ändern; Migration-018-Kommentar (24/24/3) bewusst belassen (historisch)
  - Grep-Beweis: keine hartkodierten Laufzeit-/Verlängerungs-/Fristwerte außerhalb `config/vertraege.ts` (verbleibende Treffer sind Kommentare/Beispiele, MRR-Chart-Zeitraum, Testimonial-Zitate)
  - **⚠️ Offener Widerspruch (Entscheidung Felix):** Landing wirbt mit „Keine Mindestlaufzeit · Monatlich kündbar" (`components/landing/Pricing.tsx`, `WvdClient.tsx`, `Faq.tsx`) — kollidiert mit 24/12/3 im Checkout. Sales-Copy-Entscheidung, nicht eigenmächtig geändert
- **F2 komplett — Design-System / Flagship-Engine (BF §1):**
  - Migration 024 (auf Prod ausgeführt via Supabase MCP): `branchen_profile`-Tabelle, `library_pages.branchen_profil_id`, `form_submissions.qualifizierung` (jsonb)
  - `config/branchen.ts` (8 META_KATEGORIEN) + `lib/flagship/` (types, html, css, js, sections, render, anfrage): 14-Sektionen-Skelett nach BF §1.1 mit Marker-Kommentaren (`<!-- sektion:… -->`), Token-System (Reinigung hell / Restaurant dunkel), self-contained (kein CDN, keine Google-Fonts), prefers-reduced-motion, JSON-LD (LocalBusiness/Restaurant, `<` escaped gegen `</script>`-Ausbruch — Fix aus dem Stresstest)
  - `lib/flagship/seeds.ts`: beide Flagships 1:1 als `FLAGSHIP_SEEDS` (reinigung = ReiniFix, anfrage-Funnel mit 3 Quali-Fragen; restaurant_italienisch = PANE VINO, reservierung-Funnel); Media-Slots ohne `datei` → gestaltete Platzhalter (keine webp-Assets in public/)
  - Engine-Weiche `config.engine === 'flagship'`: Demo-Route (`app/demo/[token]/route.ts`, Flagship bringt Ribbon+noindex selbst mit — kein Demo-Bar-Inject) + neue Funnel-Unterroute `app/demo/[token]/[seite]/route.ts` (anfrage/reservierung, submitZiel null → Erfolgs-Screen ohne Persistenz) + Live-Auslieferung `lib/auslieferung.ts` (Startseite indexierbar, Funnel-Slug mit echtem Submit-Ziel `/api/public/forms/{siteId}/submit`)
  - Submit-Route (BF §1.2): form_type-Whitelist (anfrage/reservierung/contact), `qualifizierung` → jsonb-Spalte + in Lead-Notification-Mail gemerged, Reservierung = Name+Telefon Pflicht (E-Mail optional, Bestätigungsmail nur wenn E-Mail vorhanden); Funnel-JS mappt `telefon`→`phone` im Payload
  - Lead-Inbox (`components/leads-view.tsx`): Qualifizierungs-Block im Detail-Modal, form_type-Badges (Anfrage/Reservierung), Telefon statt E-Mail in Listenzeile + tel:-Anrufen-Button wenn keine sender_email (Reservierung); Submissions-API liefert qualifizierung via `select('*')` automatisch
  - **DoD-Beweis:** `npm run test:flagship` (`scripts/test-flagship.ts`) — beide Seeds + je 3 abweichende Datensätze (lange Texte/Umlaut-Firma, XSS-Injektion in 8 Feldern, Minimal-Daten): 270 Prüfungen grün (alle Sektionsmarker inkl. Ablauf-Pflichtlogik, kein `undefined`/`{{…}}`, self-contained, Escaping, Quali-Fragen bzw. Reservierungsfelder im Funnel, Live-Modus ohne noindex + Submit-Ziel). Typecheck + Lint grün

- **F3 gebaut — Branchen-Seeding (BF §3/§4), Commit `83fd246`:**
  - `lib/seeding/`: `schema.ts` (BranchenSeedSchema, Zod v4, JSON-Schema ins Prompt), `branchen-start.ts` (16 Start-Branchen, reinigung + restaurant_italienisch = Flagship-Parametrisierung ohne Claude-Call), `generiere-profil.ts` (EIN Claude-Call je Branche, `claude-sonnet-4-6`, MAX_VERSUCHE=3 mit Feedback-Messages, robuster JSON-Parser gegen Prosa-Präfixe), `komponiere-vorlage.ts` (Profil → FlagshipConfig), `gates.ts` (Sektionsmarker, Floskel-Blacklist, Funnel-Felder — bei Verstoß wird NICHT gespeichert), `seed-branche.ts` (Orchestrator: Profil → Vorlage → Gates → Asset-Grundset 1 Hero + Signature-Paar + 6 Galerie → branchen_profile als draft)
  - CLI `npm run seed:branchen` (`--nur`, `--ohne-assets`, `--neu`); Admin-Freigabe-UI `/admin/branchen` (approve/draft/feedback) + Preview-Routen `/branchen-preview/[key]` (+ Funnel-Seite)
  - Seeding-Stand Prod: 8/16 als draft (reinigung, restaurant_italienisch, maler, dachdecker, cafe, kosmetikstudio, zahnarzt, physiotherapie); Rest lief zunächst auf leeres Anthropic-Guthaben — Nachlauf nach Aufladung gestartet
- **F4 gebaut — Auto-Seeding (BF §4), Commit `83fd246`:**
  - `lib/seeding/klassifiziere-branche.ts`: Freitext → StartBranche via Claude; `sicherheit: 'unsicher'` = harter Fehler statt Raten (BF §4.1)
  - POST `/api/admin/branchen` (maxDuration 800): klassifizieren → approved-Schutz (409) → seedBranche → Slack `#library` „wartet auf Freigabe" + Preview-Link; `lib/slack.ts` um Channel `library` (`SLACK_WEBHOOK_LIBRARY`) erweitert
  - Regenerier-Runde: PATCH `/api/admin/branchen/[key]` `{action:'feedback', regenerieren:true}` — Note in guideline_notes, Re-Seed mit injizierten Notes (Meta-Kategorie-weit aggregiert, BF §4.7), Slack-Ping; UI: „Feedback & neu generieren"-Button + „Neue Branche bauen"-Input in `/admin/branchen`
  - DoD-Test (3 ungeseedete Branchen ohne Handarbeit) steht aus — nach dem F3-Nachlauf
- **F5 gebaut — Demo-Pipeline (BF §6), Commit `f2cf833`:**
  - Migration 025 (additiv, auf Prod via MCP): `demos.kosten_cent` + `demos.asset_meta`
  - `lib/pipeline/generate-flagship-demo.ts`: nur aus APPROVED Vorlagen („ohne Freigabe keine Demo"); deterministische Personalisierung ohne Claude-Call (String-Walk Firma/Ort/Telefon, Meta-Ersatz, echte Google-Bewertungen ≥2 in die Stimmen, Template-Telefon nie im Kundenoutput); frische Schlüssel-Assets (Hero 16:9 + Signature-Paar 4:3, quelle `demo_generiert`); Fallback-Kette frisch → Bank (approved bevorzugt) → CSS-Platzhalter — kein Lauf scheitert an Bildern; Warnung bei > 150 Cent (DoD ≤ 1,50 €/Demo)
  - POST `/api/admin/demos` engine `flagship` (template_id `flagship:<key>`, maxDuration 120→300), GET liefert `kosten_cent`; GET `/api/admin/branchen` (Branchen-Liste für die Demo-UI); Demo-UI: Optgroup „Branchen-Fabrik (Flagship)" (nur approved), Kosten je Demo in der Liste (rot über DoD-Grenze)
  - tsc + Lint + `test:flagship` (270 Prüfungen) grün; DoD-Test braucht erste Mensch-Freigabe

### 2026-07-20 — MVP-Finish, Session 1 (Branch `feat/mvp-finish`)
- **Abschnitt 0 (Git-Sicherheitsnetz), Commit `14c89fb`:** `MVP_FINISH_PROMPT.md` ins Repo, Baseline geprüft (Build/Typecheck/Lint/Tests grün), Branch `feat/mvp-finish` von main
- **Phase 1 komplett — Multi-Tenant-Hosting auf Vercel (MVP-Finish §1):**
  - **Cloudflare-Kundendeployment entfernt** → `_legacy/lib/cloudflare.ts`, `_legacy/lib/cloudflare-email.ts`, `_legacy/app/api-sites/publish-mit-cloudflare.route.ts` + `rollback-mit-cloudflare.route.ts`; Begründung + Reaktivierungspfad in `LEGACY_NOTIZEN.md` §8; DB-Spalten (cf_*) bleiben additiv als @deprecated
  - **`lib/hosting/routing.ts`** — `entscheideRouting(host, pfad, env, override)`: Marketing-Host passthrough (+ `/dashboard` → App-Host-Redirect), App-Host (`/` → `/dashboard`, Marketing-Inhalte → Marketing-Host), `{slug}.MARKETING_HOST` + Custom Domains → Rewrite auf `/kundenseite/{host}/{pfad}`; Ausnahmen: vercel.app, localhost, `/api`, `/kundenseite`; ohne Env-Vars komplett No-op; `?__host=`-Override nur dev bzw. `ALLOW_HOST_OVERRIDE=1` (Hostname-Validierung gegen Injection). Middleware nutzt ausschließlich diese pure Funktion
  - **`app/kundenseite/[host]/[[...pfad]]/`** — Catch-all-Renderer: alle Kundenseiten aus der DB, gesperrte/unpublizierte Sites → 404/Sperrseite
  - **`lib/hosting/site-cache.ts`** — zweischichtiger Data-Cache via `unstable_cache`: Host→SiteId unter Tag `host-map` (TTL 300s), (SiteId, Pfad)→HTML unter Tag `site:{id}` (TTL 300s); `revalidateSite(id)`/`revalidateHostMap()` feuern exakt diese Tags; verdrahtet in publish, rollback, Stripe-Sperre (Dunning), Domain-Check (AKTIV) und Domain-DELETE; obendrauf CDN `s-maxage=120`
  - **`lib/hosting/vercel-domains.ts`** — `attachCustomDomain(supabase, siteId, hostname)`: Vercel Domains API v10 (`VERCEL_TOKEN`/`VERCEL_PROJECT_ID`/optional `VERCEL_TEAM_ID`), `domain_already_in_use_by_project` = bereits verifiziert; ohne Token Stub-Modus (domains-Zeile + DNS-Anleitung entstehen trotzdem) → WARTELISTE; `dnsZielFuer()` liefert Maschinenwerte (Apex `76.76.21.21`, Subdomain `cname.vercel-dns.com`), damit der DoH-DNS-Check (`pruefeDnsEintrag`) direkt vergleichen kann; POST `/api/sites/[siteId]/domains` (typ `vorhanden`) ruft jetzt attachCustomDomain statt manuellem Insert
  - **DoD-Beweis:** `npm run test:hosting` (`scripts/test-hosting.ts`) — 40 Checks grün: Teil A Routing-Weichen pur (inkl. zwei Slugs → zwei verschiedene Rewrite-Ziele, Override-Gating), Teil B Cache-Verdrahtung nachweislich (next/cache gepatcht: Render-Registrierung und Invalidierung feuern IDENTISCHE Tags → Publish invalidiert den Site-Cache), Teil C Aufrufstellen-Checks im Quelltext
  - **Nebenbefund behoben:** `.env.local` war auf 2 Keys geschrumpft (vermutlich `vercel env pull` am 16.07.) → Build brach bei Page-Data-Collection (`supabaseUrl is required`, Modul-Level-Client in `app/api/public/track/[siteId]/route.ts`, Bestand). Öffentliche Supabase-Werte (URL + anon key) via Supabase MCP wiederhergestellt; restliche Secrets fehlen weiter lokal (Vercel hat sie)
  - Aufräumer: `components/onboarding-wizard.tsx` ungenutzten `customer`-Prop entfernt (Rest der CF-Step-Entfernung)
  - Self-Test: Build + `tsc --noEmit` + Lint (nur Bestands-Warnings) + `test:hosting` (40) + `test:flagship` (463) + `test:kuendigung` grün
  - WARTELISTE neu: VERCEL_TOKEN/PROJECT_ID, Wildcard-Domain `*.<PRODUKTDOMAIN>`, `NEXT_PUBLIC_MARKETING_HOST`/`NEXT_PUBLIC_APP_HOST`
- **Phase 2 komplett — Asset-System & Higgsfield-Pipeline (MVP-Finish §3):**
  - **§3.1 Typisiertes Slot-Schema + Registrier-Gate:** Migration 026→`027_asset_slots.sql` (additiv, auf Prod via Supabase MCP): `asset_bank.aspect_ratio` + `alt_text_de`, Backfill aus breite/hoehe (±5 %). `lib/assets/slots.ts` — Zod-Schema (`scene_typ` hero/galerie/vorher/nachher/team/detail, `aspect` 16:9/4:3/3:4/1:1, `min_width`, `pair_with` nur vorher→nachher + gleicher Aspect, `pflicht`); `validiereAssetSlots()` wirft mit Sektionsname. `lib/flagship/asset-slots.ts` deklariert die Flagship-Sektionen (hero, signature-Paar, ergebnisse) und validiert **beim Import** — `lib/flagship/render.ts` importiert das Modul als Gate: invalide Deklaration = Modul lädt nicht
  - **§3.2 Nur `approved` wird je ausgeliefert:** `lib/assets/repository.ts` — `getApprovedAssets()` mit fest verdrahtetem `.eq('quality_status','approved')` (nie Aufrufer-Sache). **Verstoß im Bestand behoben:** `generate-flagship-demo.ts` bankHero/bankPaar hatten einen Fallback auf nicht-approvede Assets (`for (const nurApproved of [true,false])`) — jetzt ausschließlich über das Repository
  - **§3.3 Deterministisches `assignAssets`:** `lib/assets/assign.ts` — Seed = siteId, RNG pro (seedKey, assetId) aus FNV-1a+mulberry32 (Ranking unabhängig von Kandidaten-Reihenfolge ⇒ gleiche Site = gleiche Bilder), Aspect ±5 % aus breite/hoehe (Fallback deklarierter aspect_ratio), min_width, Style-Tag-Ranking vor RNG, keine Duplikate pro Seite, Paare IMMER komplett über pair_id (nie ein halbes Paar; beide Slots optional ⇒ ausgeblendet, sonst Fehler). Pflicht-Slot ohne Kandidat = harter Fail: `asset_bank: kein approved hero/16:9 für branche=maler` — NIE Platzhalter
  - **§3.4 Seeding-CLI + Style-Baukasten:** `config/asset-styles.ts` — feste Module (Licht, Farbwelt, Kamera) je Branche (maler/gastro + generischer Fallback) + `BASIS_NEGATIV` (keine Gesichter nah, keine Logos/Marken, kein Text im Bild) in jedem Prompt; deutsche Alt-Text-Vorlagen. `npm run seed:assets -- --branche maler --count 30` — Mix skaliert aus Basis {6 hero, 12 galerie, 2 team, 2 detail, 4 Paare}, alles als draft; ohne Key lauter STUB-Warnung (`ai_mock` nie approvebar); harter Abbruch bei Budget/Kill-Switch. Pipeline erweitert: `generiereAsset`/`makePair` persistieren jetzt `aspect_ratio` + `alt_text_de`
  - **§3.5 Freigabe-UI `/admin/assets`:** Grid mit Filtern (Branche/Szene/Status), Paar-Karten (nachher zuerst), Großansicht mit Alt-Text-Korrektur, Approve/Reject/Draft/Regenerieren. PATCH `/api/admin/assets/[id]`: Status-Wechsel wirkt server-seitig IMMER auf beide Paar-Hälften; Approve von `ai_mock` wird mit 400 abgelehnt; Regenerieren nutzt gespeicherten `gen_prompt` mit neuem Seed (Paar ⇒ komplettes `makePair`, nie halb); Kill-Switch ⇒ 409. Nav-Link „Asset-Bank" im Admin-Layout
  - **DoD-Beweis:** `npm run test:assets` (`scripts/test-assets.ts`) — 32 Checks grün: Schema-Gate (snake_case, pair_with-Richtung/Aspect), Determinismus (gleiche Site ⇒ gleiches Bild, reihenfolge-unabhängig, Streuung über Sites), keine Duplikate, Paar-Kopplung (nie halb, ausgeblendet statt halb), harter Fail mit Soll-Meldung, Aspect ±5 % + min_width, Style-Ranking schlägt RNG (4 Seeds), Negativ-Regeln im Prompt, deutsche Alt-Texte
  - Self-Test: Build + `tsc --noEmit` + Lint (nur Bestands-Warnings) + `test:assets` (32) + `test:flagship` (463) + `test:hosting` (40) + `test:kuendigung` grün
  - **DoD-Rest auf WARTELISTE (Mensch/Key):** echter `HIGGSFIELD_API_KEY`/`FAL_API_KEY` fürs Seeding (Stubs nie approvebar) + Freigabe von ≥30 Assets inkl. 3 Paaren für die erste Ziel-Branche in `/admin/assets`
- **Einschub (User-Auftrag): Apple-Red-Design-System auf Marketing-Seiten, Commit `1453fd2`:**
  - Quelle: Claude-Design-Handoff `~/Downloads/apple-red-software-system/` (Rot #E0354B, helle Flächen, weiche Schatten statt harter Borders, Apple-System-Font, Motion 120–220ms ease-out, Success-Grün #2FBF71 für Vorteile)
  - `marketing.css` komplett neue Token-Schicht + **Legacy-Aliasse** (`--blue`→red-500, `--dark`→gray-900, `--cream`→weiß, `--font-display/mono`→`--font-ui` …) — dadurch ziehen alle Unterseiten (Blog, Entwurf, Kundenmeinungen, Ergebnisse, Rechtliches) automatisch mit; alle Klassennamen unverändert
  - `(marketing)/layout.tsx`: Google-Fonts (Fraunces/Inter Tight/JetBrains) entfernt → System-Stack (LCP-Gewinn, 0 Font-Downloads)
  - `WvdClient.tsx`: dunkle Bento-/ROI-Sektionen → hell, Funnel-/Vergleichskarten als weiße Cards (Highlight = red-050), Konsequenzen-Karten im Vorteile-Stil (Success-Grün ✓ / Danger ✕), rote Sterne, Footer hell, framer-reveal 0.22s easeOut
  - Hex-Fixes: EntwurfForm (Check-Icon), KundenmeinungenPage (Sterne), ErgebnissePage (Header-Gradient → `--accent-grad`)
  - Self-Test: Build + `tsc --noEmit` + Lint (nur Bestands-Warnings) grün
  - Nicht angefasst (unbenutzte Legacy-Komponenten): components/landing/{Hero,Workflow,…}.tsx; offen: ob Design-Bundle-ui_kits (Dashboard/Builder/CustomerPortal) auch Admin-/Kunden-UI ersetzen sollen → Rückfrage an Felix
- **Einschub (User-Auftrag): Landing-Bugfix weiße Schrift/Buttons, Commit `fad72b4`:** EntwurfForm (Optionsbuttons, Progress, Erfolgsscreen, Fehlerbox), ErgebnissePage + KundenmeinungenPage Inline-Texte von Dark-Theme-Weiß auf Token-Farben umgestellt
- **Einschub (User-Auftrag): Apple-Red auf Admin-Dashboard + Kunden-Ansicht:**
  - `admin.css` + `dashboard.css`: komplette Token-Wert-Remaps auf Apple Red (Klassennamen + `--za-*`-Namen unverändert, `--za-gold` ist jetzt semantisch Rot #E0354B); Aurora → Rosé-Töne, Noto Serif → Apple-System-Stack, Motion auf 160/200/220ms cubic-bezier(.22,.61,.36,1), Success-Flächen → #E4F7EC, Danger → #B3261E
  - Inline-Blues/Noto-Serif gefixt in: admin/{page,demos,templates,potenzial-rechner}, (auth)/{login,register} (self-contained, inkl. Google-Fonts-Link entfernt), components/{site-editor,customer-sidebar,customer-welcome,help-center,analytics-dashboard}
  - Blaustichige Neutral-Overlays/Grautöne → Ink-Basis rgba(23,24,26,x); Amber-Draft-Status und Chatbot-Lila bewusst behalten
  - Self-Test: Build + `tsc --noEmit` + Lint (nur Bestands-Warning alt-text) grün

### 2026-07-21 — MVP-Finish, Session 2 (Branch `feat/mvp-finish`)
- **Einschub (User-Auftrag): Logo + Markenname, Commit `5056cab`:** Logo aus `~/Downloads` als `public/logo.svg` eingebunden (Marketing-Nav, Footer, Admin-Sidebar, Auth-Seiten); Marke durchgängig „Webseiten-Verlag Deutschland" (nicht „RedBuild")
- **Erweiterungs-Auftrag angenommen:** „QA-Gate & Produktstufen" verbatim als `QA_GATE_PRODUKTSTUFEN_PROMPT.md` ins Repo. Einsortierung: Baustein A+B (Browser-QA + QUALITY_CHECKLIST) nach Phase 3, Baustein C (Produktstufen 99/149/249 €) nach Phase 4; Reihenfolge A⇒B⇒C; nach jedem Baustein PROGRESS + Commit + Selbsttest
- **Phase 3 komplett — Ein-Klick-Demo aus dem 2-Minuten-Formular (MVP-Finish §4):**
  - **§4.1 Formular = einzige Datenquelle:** Migration `028_phase3_lead_generierung.sql` (additiv, auf Prod via Supabase MCP): `business_profiles` (lead_id, firma, branche_key, stadt, telefon, leistungen jsonb 3–8, usps, oeffnungszeiten, email, notizen; RLS admin-only) + `generation_jobs` (lead_id, business_profile_id, status laufend|demo_erstellt|demo_bereit|failed, fehler_grund, kosten_cent, copy_versuche, validator_report jsonb, demo_id, site_id) + `demos.demo_bereit/geprueft_von/geprueft_am` + `leads.demo_id`. UI `/admin/leads/neu` (Pflicht: Firma, approved-Branche, Stadt, Telefon, 3–8 Leistungen je Zeile; optional USPs/Öffnungszeiten/E-Mail/Notizen; „Direkt generieren"-Checkbox) + Zod-validierte POST `/api/admin/leads` (prüft Branche gegen `branchen_profile`)
  - **§4.2 Ein-Klick-Generierung:** `lib/generierung/lead-demo.ts` — `generiereDemoFuerLead()`: Job `laufend` → approved-Vorlage der Branche → Copy-Slots via Claude (`lib/generierung/copy-slots.ts`: striktes Zod-Schema je Sektion, Gates Floskel-Blacklist + Stadt in H1 + Stadt in seo_titel ≤60, max 2 Versuche mit Feedback, danach deterministischer Fallback aus Formulardaten — Demo entsteht IMMER) → assignAssets (nur approved, deterministisch per siteId) → Konsistenz-Validator (harter Gate) → Site+Demo+Subdomain (`baueSubdomainSlug`: Umlaute, ≤50 Zeichen) → Job `demo_erstellt` mit kosten_cent/copy_versuche/validator_report; jeder Fehler → `failed` mit menschenlesbarem `fehler_grund`. POST `/api/admin/leads/[leadId]/generieren` (maxDuration 300, Kill-Switch → 503)
  - **§4.3 Konsistenz-Validator:** `lib/generierung/konsistenz-validator.ts` (11 Regeln: fremde Städte via `staedte-blockliste.ts` TOP-200 + Bestandteil-Logik „Frankfurt"/„Halle (Saale)", Kundenstadt exempt; Floskeln; Platzhalter/undefined/{{}}; Bild-Duplikate; alt/width/height Pflicht; Paar-Vollständigkeit über pair_id; Ratio ±5 %; erfundene Bewertungen ohne `echteBewertungen`; Telefon-Konsistenz; SEO-Länge; CDN-Verbote) + `reportAlsText()` für lesbare Job-Fehler
  - **§4.4 Mensch-Gate:** `/admin/leads` Liste (Job-Status-Pills laufend/demo_erstellt/demo_bereit/failed mit fehler_grund im Klartext, Kosten €, Neu-generieren) + Checkbox „Demo geprüft" → POST `/api/admin/demos/[demoId]/freigeben` (setzt `demo_bereit`+`geprueft_von/am`, spiegelt Job-Status; abwählbar). Demos gehen NIE automatisch raus. Nav „Leads" im Closer-Block
  - **DoD-Beweis:** `npm run test:phase3` (`scripts/test-phase3.ts`) — 39 Checks grün: Städte-Blockliste (Erkennung, Kundenstadt-Ausnahme, Bestandteile, case-insensitive), Copy-Slots (Schema-Grenzen, Gates inkl. injizierter Floskel + fehlender Stadt, [[Highlight]]-Marker), Validator (jede Regel einzeln getriggert inkl. `meta=null`-Pfad, Ratio-Toleranz, echte Bewertungen erlaubt), Subdomain-Slugs
  - Self-Test: Build + `tsc --noEmit` + Lint (nur Bestands-Warnings) + `test:phase3` (39) + `test:flagship` (463) + `test:hosting` (40) + `test:assets` (32) + `test:kuendigung` grün
  - **DoD-Rest auf WARTELISTE (Mensch/Key):** 5-Testfirmen-Lauf braucht `ANTHROPIC_API_KEY` lokal + approvede Assets der Ziel-Branche; Lighthouse ≥90 auf generierten Demos erst danach messbar
- **Baustein A komplett — Browser-QA-Gate mit chirurgischer Selbstreparatur (QA_GATE_PRODUKTSTUFEN_PROMPT §A):**
  - **Regel-Registry:** `lib/qa-gate/regeln.ts` — ~30 `QaRegel` (id, beschreibung, layer config|render|browser, autofix, implementiertIn, marker, testRef, kategorie) als einzige Wahrheitsquelle; `RegelErgebnis`/`QaReport`-Typen + `fehlerAlsText()` für lesbare Reports
  - **Render-Checks (ohne Browser):** `lib/qa-gate/render-checks.ts` — R-VARIANTEN (original_-Pfade), R-CDN (nur Supabase-Hosts), R-JS-BUDGET (32 KB Inline-JS), R-ALT, R-SEO-TITLE (Stadt in Title+Description), R-JSONLD, R-OG, R-SITEMAP (publish: canonical Pflicht), C-VIDEO-SIZE (≤3 MB, untranscodiert = Fail), C-VIDEO-FALLBACK (Poster/Bild)
  - **Browser-Checks (Playwright):** `lib/qa-gate/browser-checks.ts` — B-BLOCKLISTE (fremde Städte im DOM), B-STUB, B-KONTAKT, B-IMG-LOAD (naturalWidth), B-ASPECT, B-CONSOLE, B-CLS, B-LINKS, B-FORM, B-MOTION, B-RECHT, B-NOINDEX, B-VIDEO-ATTR, B-VIDEO-LAZY — mobil 390×844 + Desktop 1440×900, Screenshots → Storage
  - **Chirurgische Reparatur:** `lib/qa-gate/reparatur.ts` — `fuehreQaLaufAus()`: max 2 Runden, NIE globale Regeneration. Bild-Fehler ⇒ Ersatz-Kandidat aus Asset-Bank (defektes Asset markiert); Copy-Fehler ⇒ `generiereSlots(feedback)` und Übertrag NUR betroffener Felder (Rest byte-identisch); Alt-Fehler ⇒ Alt aus Asset-Bank bzw. Fallback „Label – Firma in Stadt"; Video-Fehler ⇒ Video raus, Bild bleibt + manual_task. Strukturelle Klassen (B-CLS, B-CONSOLE, B-KONTAKT, …) ⇒ sofort `failed` (Runde 0) + manual_task; Endlosschleifen-Guard
  - **Orchestrierung:** `lib/qa-gate/browser-qa.ts` (`browserQa(siteId, mode demo|publish|cron)`: Render → Reparaturlauf → Report + Screenshots → `qa_reports`, Migration 029 additiv auf Prod); `lib/qa-gate/publish-qa.ts` (`starteAsyncPublishQa`: blockiert Kunden NIE, kein Rollback, Fail ⇒ manual_task)
  - **Integrationen:** Demo-Generierung `lead-demo.ts` (QA vor demo_erstellt), Freigabe-Route hartes Gate (nur passed/repaired ⇒ demo_bereit), Publish + Kunden-Freigabe (async QA), manueller Lauf POST `/api/admin/sites/[siteId]/qa` (Admin „QA prüfen" mit Screenshots in `/admin/leads`), Nacht-Scan `/api/cron/qa-scan` (02:00, max 20 Sites, vercel.json)
  - **Bugfix Städte-Blockliste (Produktions-Falsch-Positiv):** Konjunktion „oder" matchte Frankfurt (Oder) — `staedte-blockliste.ts` matcht mehrdeutige Namensteile (`oder`, `essen`, `halle`, `hof`, `löhne`, `bünde`) jetzt nur noch case-sensitiv; test-phase3 (Halle-Kernname, KÖLN case-insensitiv) weiter grün
  - **DoD-Beweis (Golden-Set):** `npm run test:qa-gate` (`scripts/test-qa-gate.ts`) — 68 Checks grün: Teil A alle Render-Regeln einzeln getriggert; Teil B Selbstreparatur (kaputtes Bild ⇒ Ersatz + defekt-Markierung, Stub-Slot ⇒ nur betroffenes Feld neu, fremde Stadt, fehlender Alt inkl. Fallback, kaputtes Video ⇒ Bild-Fallback + Task, unreparierbar ⇒ `failed` nach 2 Runden mit lesbarem Grund, kein Ersatz ⇒ `failed`, sauberer Lauf ⇒ `passed` byte-identisch); Teil C alle 9 strukturellen Klassen ⇒ `failed` + manual_task
  - Self-Test: Build + `tsc --noEmit` + Lint (nur Bestands-Warning alt-text) + `test:qa-gate` (68) + `test:phase3` (39) + `test:flagship` (463) + `test:hosting` (40) + `test:assets` + `test:kuendigung` grün
  - **WARTELISTE:** `BROWSER_QA_WS_ENDPOINT` für Prod (Vercel hat kein lokales Chromium; lokal reicht `npx playwright install chromium`)
- **Baustein B komplett — QUALITY_CHECKLIST.md + Coverage-Lint (QA_GATE_PRODUKTSTUFEN §B):**
  - `QUALITY_CHECKLIST.md` (Repo-Root) wird aus der Registry `lib/qa-gate/regeln.ts` generiert (33 Regeln, gruppiert nach Kategorie, je Regel ID/Beschreibung/Layer/Autofix/Implementierung/Test) — Registry ist die einzige Quelle, Datei trägt „nicht von Hand editieren"
  - `scripts/check-quality-coverage.ts`: (1) ID-Eindeutigkeit + Layer-Präfix (C-/R-/B-), (2) Marker muss wörtlich in `implementiertIn` vorkommen, (3) Regel-ID muss in `testRef` vorkommen, (4) QUALITY_CHECKLIST.md muss synchron sein (Drift = Fehler). Regel ohne Implementierung ⇒ exit 1 = **Build-Fehler** (`npm run build` läuft den Check jetzt VOR `next build`); Regenerieren via `npm run check:quality -- --write`
  - Negativ-Beweis: vor dem Tagging schlug der Check korrekt mit 9 Fehlern fehl („Regel-ID nicht referenziert") — bestehende Tests in test-phase3/test-assets wurden an den Assert-Stellen mit ihren Regel-IDs getaggt (`[C-LIMITS]`, `[C-STADT-H1]`, `[R-FLOSKEL]`, `[C-SLOTS]`, `[R-IMG-DIM]`, `[C-IMG-DUP]`, `[C-PAAR]`, `[C-REVIEWS]`, `[C-APPROVED]`)
  - Self-Test: Build (inkl. Coverage-Gate) + `tsc --noEmit` + `check:quality` (33 Regeln) + `test:qa-gate` (68) + `test:phase3` (39) + `test:assets` grün
- **Phase 4 komplett — Kundenportal & Chat-Editor gehärtet (MVP-Finish §5):**
  - **§5.1 Bild-Swap auf die Bank festgenagelt:** Op `swap_image_from_pool_or_upload` (URL-basiert, Host-Allowlist) ersetzt durch `swap_image_from_bank` (Zod: UUID-`assetId`). Die Chat-Route lädt vorab die erlaubte Menge (`getEditorAssets` in `lib/assets/repository.ts`: approved Branchen-Bank OHNE quelle='kunde' + eigene Kunden-Uploads dieser Site) über den Admin-Client (asset_bank ist RLS-admin-only) und übergibt sie als `Map<assetId, AufgeloestesBild>` an das synchrone, atomare `applyPatch`. Unbekannte IDs ⇒ verständliche Abweisung; Szene-Kompatibilität: Hero-Pfade nur Szene `hero`, `ownerImageUrl`/`teamImageUrl` nur team/detail/galerie (`erlaubteSzenenFuerPfad`). Der LLM-Prompt (`getOpsPrompt(bildListe)`) listet NUR verfügbare IDs (`formatiereBildListe`, max 40, Kunden-Uploads markiert); `lib/claude.ts` fädelt die Liste in Single-/Multi-Page-Prompts
  - **§5.2 Kunden-Upload mit Sharp-Pipeline:** `app/api/customer/bilder` POST: EXIF-`rotate()` → `resize(max 2560, withoutEnlargement)` → WebP q82 — nie Originale ausliefern; unlesbare Datei ⇒ 400 mit klarer Meldung. Aspekt-Check `lib/assets/aspekt.ts` (Config `SLOT_ASPEKTE`: hero 16:9±20 %, galerie 4:3±35 %, detail 4:3±45 %, team 1:1±35 %): Antwort enthält `aspekt` mit `kompatibleSlots` + zentrierten Auto-Crop-Vorschlägen (`zentrierterCrop`, Pixel-genau — nie stilles Verzerren). Upload landet zusätzlich als `quelle='kunde'` in der asset_bank (Migration 030: `site_id`-Spalte, additiv, via Supabase MCP ausgeführt) — site-gebunden; `getApprovedAssets` schließt `quelle='kunde'` jetzt IMMER aus (Kundenbilder nie in fremden Demos). DELETE räumt den Bank-Eintrag mit ab
  - **§5.3 Gesperrte Kern-Sektionen + Begründung:** `GESPERRTE_PFADE`/Hero-Toggle-Sperre bestanden schon; NEU im Ops-Prompt: Regel „GESPERRTE BEREICHE … NIE einen Op versuchen, stattdessen BEGRÜNDEN + konkrete ALTERNATIVE anbieten". Draft→Preview→Publish→Rollback + Rate-Limit 50/Tag waren vorhanden (verifiziert: `site-editor.tsx` HistoryTab, publish-Route, `CHAT_LIMIT_PRO_TAG`)
  - **§5.4 Rechtstexte als Config-Vorlage:** Alle statischen Bausteine aus `lib/rechtstexte.ts` nach `config/rechtstexte-vorlagen.ts` gezogen (IMPRESSUM_VORLAGE/DATENSCHUTZ_VORLAGE); Generator setzt nur noch Pflichtangaben ein. Generator+Wizard+Auslieferung (/impressum, /datenschutz, nicht löschbar, rechtstexte-Pfad gesperrt) bestanden schon. WARTELISTE: Anwalts-Review der Vorlagen
  - **DoD-Beweis:** `npm run test:phase4` (`scripts/test-phase4.ts`) — 45 Checks grün: Schema (Bank-Op ja; alter Op/Roh-HTML/Nicht-UUID nein), Bild-Leitplanken (unbekannte ID verständlich abgewiesen, Galerie-Asset nicht auf Hero, Kunden-Upload auf ownerImageUrl, ohne Map kein Swap, All-or-Nothing, Immutabilität), Sperren (Telefon/Rechtstexte/Hero-Toggle), Aspekt (16:9-Erkennung, Crop 16:9→1:1 zentriert 1080×1080@left=420, Hochformat-Crop), Rechtstexte (PLZ/E-Mail-Validierung, Vorlagen-Bausteine im Output), Prompt-Vertrag (IDs + Kunden-Markierung, Begründungs-Regel, alter Op-Name weg). `smoke-editor-ops` auf den neuen Op umgezogen, weiter grün
  - Self-Test: Build (inkl. Coverage-Gate, Marker C-APPROVED erhalten) + `tsc --noEmit` + Lint (nur Bestands-Warning alt-text) + `test:phase4` (45) + `test:qa-gate` (68) + `test:phase3` (39) + `test:flagship` (463) + `test:hosting` (40) + `test:assets` + `test:kuendigung` grün

- **Baustein C komplett — Produktstufen 99/149/249 € (QA_GATE_PRODUKTSTUFEN §C):**
  - **§C.4 Feature-Matrix als einzige Wahrheit:** `config/plans.ts` erweitert um `EditorFeature`-Typ + Rechte-Matrix je Tier (Starter: update_text/swap_image_from_bank/set_theme_preset; Business: +add_section_from_library/reorder/signature_section; Growth: +video_header/scroll_story), `preis_hinweis` ('99/149/249 € netto'), Starter-`erlaubteThemePresets` (klar-blau, modern-anthrazit, warm-terracotta) und Helfer `hatEditorFeature`/`kleinsterPlanMitEditorFeature`. `config/stripe-produkte.ts`: Env-Mapping Tier→Stripe-Price-ID (`STRIPE_PRICE_STARTER/BUSINESS/GROWTH`); `lib/stripe.ts` nutzt gepflegte Price-ID, sonst bestehenden price_data-Fallback → WARTELISTE „Stripe-Produkte anlegen"
  - **§C.2 Server-Gate + Editor-als-Verkäufer:** `lib/editor-ops.ts` — `pruefePlanRecht(tier, op)` liefert verkäuferische Upsell-Antwort (Nutzen + Zielpaket + Preis aus Config), `applyPatch(..., tier?)` weist gesperrte Ops VOR Anwendung ab (ohne Tier kein Gate = Abwärtskompatibilität für Bestands-Aufrufer/Smoke); Starter-Preset-Allowlist beim `set_theme_preset`; NEU: Kern-Sektionen-Fix beim `reorder` (Hero/CTA/Kontakt behalten Position, nur bei gesetztem Plan aktiv); `getOpsPrompt(bildListe, tier)` stellt „PAKET-RECHTE"-Baustein voran (gesperrte Ops NIE versuchen, stattdessen Upsell-Antwort). Chat-Route übergibt `customerContext.paket` — Gate hängt NIE nur an der UI
  - **§C.1 Frozen Composition:** Migration `031_frozen_composition.sql` (additiv, auf Prod via Supabase MCP): `library_pages.frozen boolean` — Starter-Vorlagen mit fixer Komposition; Starter-Editor kann ohnehin nur Text/Bild/Preset (Matrix)
  - **§C.3 Video-Leiste (Growth):** `config/video-guidelines.ts` (5–8s Loop, ≤3 MB, Technik-/Negativ-Regeln, 17 Branchen-Stile, `buildVideoRefinerPrompt` deutsch→englischer Video-Prompt). Admin-Werkzeug `/admin/sites/[siteId]/video` + API `/api/admin/sites/[siteId]/video`: refine (Claude, Original vs. verfeinert editierbar) → generate (image-to-video vom Hero-Bild, stub-fähig — Kette bleibt ohne Provider testbar) → asset_bank-Draft (medium='video', gen_prompt/gen_seed/gen_job_id/kosten_cent) → Freigabe mit Loop-Vorschau → Zuweisung NUR approved (C-VIDEO-APPROVED, 409) + NUR Growth (`hatEditorFeature('video_header')`, 403) + immer Poster-Fallback (C-VIDEO-FALLBACK); >3 MB ⇒ Warnung (C-VIDEO-SIZE); Regenerate = gespeicherter Prompt + neuer Seed. Demos rendern Business-Level (`FlagshipRenderOptionen.level`, Video wird gestrippt), `?level=growth` zeigt den Video-Look; Live-Seiten unverändert
  - **§B-Anschluss:** 5 neue Regeln in `lib/qa-gate/regeln.ts` (Kategorie „Produktstufen", Layer config): C-PLAN-GATE, C-STARTER-PRESETS, C-COPY-PERSONAL (Copy-Slots in JEDEM Tier personalisiert — kein Duplicate Content, Pipeline hat keinen Tier-Schalter), C-STARTER-FROZEN, C-VIDEO-APPROVED; QUALITY_CHECKLIST.md regeneriert (38 Regeln), Coverage-Gate grün
  - **DoD-Beweis:** `npm run test:baustein-c` (`scripts/test-baustein-c.ts`) — 38 Checks grün: Matrix + Preis-Hinweise, Starter-Gates (add_section/toggle/reorder gesperrt mit Zielpaket+Preis in der Antwort), Business erlaubt, Kern-Sektionen-Fix (Kontakt nicht verschiebbar, freie Sektionen ja), Preset-Allowlist, Prompt-Vertrag (PAKET-RECHTE nur bei eingeschränkten Plänen), Copy-Personalisierung (Firma+Stadt im Prompt, `enthaeltStadt`), Frozen-Migration, Video-Guidelines + Refiner-Prompt, approved-only-Zuweisung
  - Self-Test: Build (inkl. Coverage-Gate 38 Regeln) + `tsc --noEmit` + Lint (nur Bestands-Warning alt-text) + `test:baustein-c` (38) + `test:phase4` (45) + `test:qa-gate` (68) + `test:phase3` (39) + `test:flagship` (463) + `test:hosting` (40) + `test:assets` + `test:kuendigung` + `smoke-editor-ops` grün
  - **WARTELISTE:** Stripe-Price-IDs (3 Produkte) im Dashboard anlegen + Envs setzen; Video-Provider-Key für echte Generierung (Stub deckt die Kette ab)

- **Phase 5 komplett — Stripe Testmode + Zugang + SEO-Upsell (MVP-Finish §6):**
  - **§6.1 Checkout erweitert (`lib/stripe.ts`):** `payment_method_types: ['card', 'sepa_debit']` (deutsche Handwerks-Kunden), Setup-Fee als einmaliger One-time-Posten aus Config (`setupFee` je Tier in `lib/packages.ts`, aktuell überall 0 = kein Posten — Mechanik getestet), Metadata additiv erweitert um `lead_id`/`site_id`/`plan` (Bestands-Schlüssel `demo_id`/`paket` bleiben, `subscription_data.metadata` gespiegelt), Laufzeit-Text weiterhin NUR aus `config/vertraege.ts` (`vertragsKonditionenText`, 24/12/3 — kein Hardcode im Stripe-Code)
  - **§6.2 Webhook-Idempotenz:** Migration `032_processed_webhook_events.sql` (additiv, via Supabase MCP auf Prod ausgeführt): `event_id text PRIMARY KEY`. Webhook (`app/api/webhooks/stripe/route.ts`) prüft die Event-ID VOR der Verarbeitung (Duplikat ⇒ sofort 200), Marker wird erst NACH erfolgreicher Verarbeitung geschrieben (`antwort.status === 200` — 500 ⇒ Stripe-Retry kann heilen); fachliche Zweitschicht (Demo `CONVERTED`, Order-Status) bleibt als Fallback bestehen. Graceful Degradation: fehlende Tabelle blockiert nichts, Insert-Fehler wird nur geloggt
  - **§6.2 Zugang per Magic-Link:** `checkout.session.completed` erzeugt via `supabase.auth.admin.generateLink({ type: 'magiclink' })` einen Login-Link und verschickt ihn mit neuem `sendMagicLinkEmail` (`lib/email.ts`) — ohne `RESEND_API_KEY` als `[MAIL-STUB]`-Log (DoD lokal nachweisbar). Dreistufige Resilienz: Magic-Link-Mail → Passwort-Fallback (`sendInvitationEmail` mit Temp-Passwort) → `MAIL_FEHLGESCHLAGEN`-Task
  - **§6.2 Dunning zeitbasiert statt event-basiert:** Neuer Zeitplan `DUNNING_ZEITPLAN` in `config/vertraege.ts` (mahnTage [0, 3, 7], Sperre ab Tag 14 — einzige Quelle) + `lib/dunning.ts` (`tageUeberfaellig`, `stufeFuerTage`, `sperreFaellig`). Webhook setzt die Mahnstufe jetzt aus Tagen seit `zahlung_ueberfaellig_seit` (nur aufwärts — Event-Doppel/Stripe-Retries idempotent; das alte `+1` pro Event ist raus) und sperrt NICHT mehr selbst. NEU: Dunning-Cron `/api/cron/dunning` (täglich 8:00, `CRON_SECRET`, vercel.json): eskaliert Tag 3 ⇒ Stufe 2 (Mahnung), Tag 7 ⇒ Stufe 3 (letzte Mahnung), Tag 14 ⇒ Site(s) `gesperrt` + `revalidateSite` + Sperr-Mail (Text-Stufe 4, DB bleibt 0–3 — Admin-UI unberührt) + `DUNNING_ESKALIERT`-Task; Sperre nur beim Übergang (bereits gesperrte Sites übersprungen). `invoice.paid` entsperrt weiterhin
  - **§6.3 SEO-Unterseiten-Abo verifiziert (bestand schon, jetzt getestet):** Kauf über `config/upsells.ts` (`seo-unterseiten-abo`) ⇒ `handleUpsellCheckout` legt eigenen Vertrag mit `upsell:`-Kennung an ⇒ Monats-Cron `/api/cron/seo-plan` (1. des Monats, Kill-Switch `generierungGesperrt`, idempotent pro Site+Monat) generiert 1 Keyword-Unterseite als `WARTET_AUF_FREIGABE` ⇒ 1-Klick-Freigabe (`/api/sites/[siteId]/seo/[lpId]`) setzt `FREIGEGEBEN` + `freigegeben_am`
  - **DoD-Beweis:** `npm run test:phase5` (`scripts/test-phase5.ts`) — 35 Checks grün: Checkout (Mode/Zahlarten/Setup-Fee/Metadata/Konditionen aus Config), Idempotenz (Migration, Prüf-Reihenfolge, Marker-Timing, Zweitschicht), Magic-Link (generateLink, Mail, Stub, Fallback), Dunning (Config-Zeitplan, Tages-Mathe, Stufen-Mapping, Sperre exakt ab 14, Webhook ohne `+1`, Cron-Verdrahtung inkl. vercel.json), SEO-Kette (Produkt, Vertrag, Cron, Freigabe). Der echte Testmode-Durchlauf braucht nur noch Stripe-Test-Keys ⇒ WARTELISTE
  - Self-Test: Build (inkl. Coverage-Gate) + `tsc --noEmit` + Lint (nur Bestands-Warnings) + `test:phase5` (35) + `test:baustein-c` (38) + `test:phase4` (45) + `test:qa-gate` (68) + `test:phase3` (39) + `test:flagship` (463) + `test:hosting` (40) + `test:assets` + `test:kuendigung` grün
  - **WARTELISTE:** Stripe-Test-Keys + `STRIPE_WEBHOOK_SECRET` + Testmode-Durchlauf; Resend-Domain für echte Mails (bis dahin Log-Stub)

- **Phase 6 komplett — Qualitäts-CI (MVP-Finish §7):**
  - **§7.1 Flagship-Golden-Set:** `test/golden_set.json` um 8 Edge-Case-Profile (`profile`-Array, BusinessProfil-Format) erweitert — Pflicht-Edges langer Name (GmbH & Co. KG), 3 Leistungen, 10 Leistungen, Umlaute/ß im Namen (Osteria Größenwahn), Bindestrich-Stadt (Castrop-Rauxel), keine Bewertungen (Stimmen-Sektion muss entfallen), zweite Branche, Umlaut+Bindestrich-Stadt (Übach-Palenberg); zwei Branchen (reinigung + restaurant_italienisch), Leistungs-Spannweite 3–10
  - **`scripts/ci-golden-set.ts` Teil 2:** generiert alle 8 Profile OFFLINE (API-Keys hart gelöscht) über die gespiegelte Produktions-Kette `personalisiereOffline` (identische Ersetzungs-Reihenfolge) → `deterministischeSlots` (statt Claude) → `wendeCopySlotsAn` → `fuelleAssetSlots` (Pflicht-Bild-Slots wie Phase-2-Asset-Zuweisung, 1600×900) → `renderFlagshipPage` → `validiereKonsistenz` (alle 11 Regeln) + Zod-Schema + Copy-Gates + Floskel-Blacklist auf dem HTML + Stadt-Check (`findeFremdeStaedte`) + CDN-Verbote; Edge-spezifische Beweise: exakt 3 bzw. 10 Leistungs-Karten, leere Stimmen-Sektion bei `keine_bewertungen`. Ergebnis: „10 Firmen (Library) + 8 Profile (Flagship), 0 Verstöße"
  - **Produktions-Bug gefunden & behoben (der Golden-Set hat sich sofort bezahlt gemacht):** Restaurant-Seed hat `meta.ort='Berlin-Friedrichshagen'`, die Seed-Texte enthalten aber auch „Berlin Friedrichshagen" (Leerzeichen) und „12587 Berlin" — die exakte Paar-Ersetzung ließ bei JEDER Nicht-Berlin-Demo „Berlin" im Kundenoutput stehen (`fremde_stadt`-Verstoß). Fix an der Wurzel: neuer Export `ortErsetzungsPaare(vorlagenOrt, neuerOrt)` in `lib/pipeline/generate-flagship-demo.ts` (exakt + Leerzeichen-Variante + Basis-Stadt, längste zuerst), verdrahtet in BEIDEN Produktions-Pfaden (`personalisiereFlagshipConfig` + `generiereFlagshipDemo`); CI importiert dieselbe Funktion (kein Drift)
  - **§7.2 Lighthouse-CI verifiziert (bestand aus Phase H):** `scripts/ci-lighthouse-render.ts` rendert je Golden-Kategorie eine Demo (`golden-<kategorie>.html`), `lighthouserc.json` mit Error-Budgets Perf ≥0.90 / SEO ≥0.95 / A11y ≥0.90 / JS ≤30 KB (30720 Bytes), Alias `ci:lighthouse` = Render + `@lhci/cli autorun`; lokaler Render-Lauf grün (11 Seiten)
  - **§7.3 Playwright-E2E verifiziert (bestand aus Phase 5/H):** `e2e/journey.spec.ts` — komplette Journey inkl. Schritt 11 Chat-Edit, 12 Publish (`status === 'published'`), 13 Rollback über `config_versions` + `/rollback`; Testmode-Guard bricht ohne `sk_test_`-Key hart ab; Alias `test:e2e`
  - **§7.4 Fehler-Handling verifiziert (bestand):** Kill-Switch `GENERATION_KILL_SWITCH` → `generierungGesperrt()` (`lib/monitoring.ts`, in Seeding/Demo/Cron verdrahtet) + `meldeJobFehler`; Job-Fails persistieren lesbaren `fehler_grund` (`lib/generierung/lead-demo.ts`) und `/admin/leads` zeigt ihn bei failed-Jobs im Klartext
  - **DoD-Beweis:** `npm run ci:golden-set` (10 Firmen + 8 Profile, 0 Verstöße) + NEU `npm run test:phase6` (`scripts/test-phase6.ts`, 28 statische Regressions-Checks über §7.1–§7.4: Golden-Set-Struktur, CI-Kette, Orts-Varianten-Fix, Lighthouse-Budgets exakt, E2E-Schritte, Kill-Switch/fehler_grund)
  - Self-Test: Build (inkl. Coverage-Gate) + `tsc --noEmit` + Lint (nur Bestands-Warning alt-text) + `ci:golden-set` + `ci-lighthouse-render` (11 Seiten) + `test:phase6` (28) + `test:phase5` (35) + `test:baustein-c` (38) + `test:phase4` (45) + `test:qa-gate` (68) + `test:phase3` (39) + `test:flagship` (463) + `test:hosting` (40) + `test:assets` + `test:kuendigung` grün
  - **WARTELISTE (bestand schon):** voller `ci:lighthouse`-Autorun + GitHub-CI erst nach Git-Remote/Push; Lighthouse ≥90 auf generierten Demos erst nach 5-Testfirmen-Lauf; echte Golden-Set-Firmen von Felix

- **Gesamtabnahme §8 — Offline-Teil (alles ohne Keys Mögliche):**
  - **NEU bewiesen — Lighthouse-Budgets real bestanden:** `npm run ci:lighthouse` diesmal KOMPLETT inkl. `@lhci/cli autorun` gegen lokales Chrome (bisher lief lokal nur der Render): 11 Seiten (8 Library-Kombis + 3 Golden-Demos), alle Error-Assertions grün — Perf ≥0.90, SEO ≥0.95, A11y ≥0.90, JS ≤30 KB. §8.2-Teilnachweis „Lighthouse ≥ 90/95/90" für die Engine erbracht; auf GENERIERTEN Flagship-Demos erst nach 5-Testfirmen-Lauf messbar (WARTELISTE)
  - **§8.1–8.2 (Formular → Demo):** Kette offline bewiesen über `ci:golden-set` (8 Edge-Profile: richtige Stadt überall via Stadt-Check + `ortErsetzungsPaare`, Bilder-Regeln via Konsistenz-Validator, Paar-Zwang, Duplikat-Check). Echter Online-Lauf „Malermeister Beispiel, Frankfurt" blockiert durch: `ANTHROPIC_API_KEY` (Copy-Slots), echten Bild-Key + Mensch-Freigabe ≥30 Assets, Branchen-Freigabe `maler` in `/admin/branchen` (bewusst NICHT selbst approved — Mensch-Gate bleibt Mensch-Gate)
  - **§8.3–8.5 (Stripe → Magic-Link → Chat-Edit → Publish → Rollback → Dunning):** als env-gated E2E implementiert; Guard-Verhalten heute verifiziert: `E2E_ENABLED=1` ohne Keys ⇒ sauberer Skip mit Fehlend-Liste, Live-Key (`sk_live_`) ⇒ harter Throw „Lauf gegen Live-Keys ist verboten". Mechanik einzeln testgedeckt (Rollback in `test:hosting`/journey, Dunning-Mathe+Cron in `test:phase5`, Magic-Link-Stub in `test:phase5`); Testmode-Durchlauf braucht nur noch `sk_test_`-Keys + Test-Supabase (WARTELISTE)
  - **§8.6 (WARTELISTE vollständig?):** Abgeglichen — alle 6 geforderten Punkte vorhanden: Vercel-Wildcard-DNS (Hosting-Block), Higgsfield-Key (F1/Asset-Bank), Stripe-Live (Entscheidungen), Resend-Domain (Sofort), Anwalt AGB/Rechtstexte (Später), Asset-Freigaben (Asset-Bank + Branchen)
  - **CI-Workflow verifiziert:** `.github/workflows/ci.yml` (Lint, Typecheck, Golden-Set, Lighthouse) liegt bereit, greift nach erstem Push
  - **Fazit:** MVP-Gesamtabnahme ist offline maximal ausgeschöpft. Der verbleibende Rest ist ausschließlich Mensch/Key: Stripe-Test-Keys + Webhook-Secret, `ANTHROPIC_API_KEY`, Bild-Provider-Key, Freigaben in `/admin`, Git-Remote

- **Auftrag T1 komplett — GaLaBau-Flagship als Komposition „galabau-landing-v1" (Branch `feat/template-galabau`):**
  - **T1.1 Port:** `GalabauLanding.dc.html` 1:1 als eigenständige Komposition portiert — `lib/flagship/galabau/` (types, sections mit 11 `g-*`-Sektionsmarkern, css, js, seed, render). Dispatch in `renderFlagshipPage`: `istGalabauKomposition()`-Type-Guard ⇒ `renderGalabauLanding()`; Bestands-Flagship unberührt (`test:flagship` 463 weiter grün). Self-contained: Inline-CSS/JS, keine CDNs, `frozen`-Komposition
  - **T1.2 Font-Fix:** SF Pro ist NICHT im Repo (Apple-Lizenz ⇒ LEGACY_NOTIZEN.md); Ersatz Inter Tight self-hosted (`public/fonts/inter-tight/`, woff2, `font-display: swap`)
  - **T1.3 Theme-Layer:** `lib/flagship/themes/galabau.ts` — Grün-Token (#3FA463-Welt) als Config, kein Hardcode in Sektionen
  - **T1.4 Copy-Slots:** `galabau/copy-slots.ts` — Limits Original +15 %, `pruefeCopySlots()` meldet `zu_lang`/`fehlt` mit Pfad; `GALABAU_VERBATIM_FELDER` (Firma/Ort/Telefon NIE durch LLM)
  - **T1.5 Asset-Slots:** `galabau/asset-slots.ts` — 21 Slots exakt nach Auftrag (hero_bg 16:9/1920 + hero_video optional mit Fallback hero_bg, about_detail, svc_01–05, why_1–3, contact_img, ba_before/ba_after als `pair_with`-Paar 4:3, team_1–3 3:4 `kunde_bevorzugt`, avatar_1–4 1:1 optional) + `slotKeyAusDateiname()` (Bindestrich-Dateiname ⇒ Slot-ID, Vertrag für T2-Import); Registrierungs-Gate validiert Deklarationen beim Modul-Load
  - **T1.6 Animations-Grammatik erhalten:** fadeUp-H1-Wortsplit-Stagger, Ken-Burns, Floaty, Sticky-Stack (top 90–154px), Clip-Path-BA-Slider mit einmaligem Hinweis-Wobble, KPI-Count-up, FAQ-Akkordeon; `prefers-reduced-motion` schaltet ALLES ab; Reveal-Gating `html.js .rv` — No-JS-Seiten bleiben voll sichtbar (SEO/Lighthouse-sicher)
  - **T1.7 Stresstest:** `npm run test:galabau` (`scripts/test-galabau.ts`) — 130 Checks grün: Seed verbatim (GrünWerk/Wiesbaden/0611 000000), 11 Marker, Copy-/Asset-Slot-Verträge, Produktstufen (business strippt Video, growth rendert `autoplay muted loop playsinline`), 3 fiese Datensätze (sehr langer Firmenname GmbH & Co. KG, nur 3 Leistungen, Castrop-Rauxel — auch im JSON-LD), Script-Injection escaped, noindex-Default (Demos) + `noindex:false` (Live), Demo-Ribbon nur mit Flag
  - **DoD-Beweis:** Playwright-Screenshots pixelnah in `docs/screenshots/galabau/` (Desktop 1440 + Mobile 390 fullPage, Hero-Viewport); Lighthouse Mobile **100/92/100** (Budget 90/95/90) auf gerendertem Seed; Self-Test grün: Build + `tsc --noEmit` + Lint (nur Bestands-Warnings) + `test:galabau` (130) + `test:flagship` (463)
  - **Screenshot-Harness-Hinweis:** fullPage-Captures zeigen Chromium-Stitching-Artefakte beim fixed Header (kein DOM-Bug, nur 1 `[data-kopf]`); Harness braucht `.rv{transition:none!important}` + langsamen Scroll, sonst Opacity-Transitions unfertig im Bild
  - **Offen (T2/T3):** `scripts/import-assets.ts` + erste GrünWerk-Demo; Higgsfield-Assets ⇒ WARTELISTE

## Template-Fabrik — Fortschritts-Matrix (einzige Wahrheit, siehe TEMPLATE_FABRIK_MASTER.md)

| Branche | B1 | Freigabe | B2 | B3 | B4 | B5-Optik | Assets | Demo-URL |
|---|---|---|---|---|---|---|---|---|
| 1 galabau | ✅ (T1-Seed) | ✅ | ✅ (`galabau-landing-v1`) | ✅ (`rezepte/REZEPTE_GALABAU.md`) | ✅ (test:galabau 130, LH 100/92/100) | ✅ (docs/screenshots/galabau/) | ⛔ Higgsfield-Key (WARTELISTE) | — (T3 offen) |
| 2 maler | ✅ (`branchen/maler/STECKBRIEF.md`) | ✅ **[x] Felix 2026-07-21** | ✅ (`maler-landing-v1`) | ✅ (`rezepte/REZEPTE_MALER.md`) | ✅ (test:maler 204, LH 100/92–95/100 auf allen 10 Seiten) | ✅ **[x] Felix 2026-07-22** (docs/screenshots/maler/, `frozen:true`) | ⛔ Higgsfield-Key (WARTELISTE) | — |
| 3–16 | — | — | — | — | — | — | — | — |

Kosten-Log je Branche (Cent): galabau 0 (Stub-Phase) · maler 0 (Stub-Phase) · scrub-premium/pv **232 Higgsfield-Credits** (Asset-Lauf 2026-07-22: 7 Entry Still + 5×45 Videos, Reserve 14 unangetastet)

## Arbeitsblock: GaLaBau-Rezeptliste komplettiert — Motiv-Prompts nach Maler-Muster

- **Anlass:** Auftrag-B-Stresstest (2026-07-22) fand als einzige Lücke: `rezepte/REZEPTE_GALABAU.md` war das einzige Rezept ohne Motiv-Prompts (nur Vertrag). Jetzt nach dem Muster von `rezepte/REZEPTE_MALER.md` ausformuliert — reine Doku, keine Engine-Datei angefasst (galabau bleibt frozen).
- **Stil-Modul ergänzt:** `config/asset-styles.ts` hat keinen galabau-Eintrag (fallbackStil greift) ⇒ Rezept definiert den verbindlichen Stil selbst, abgeleitet aus der Seed-Stimmung „Gartenanlage am Abend": golden hour, Pflanzengrün + Natursteintöne, `BASIS_NEGATIV`-Verweis.
- **Motiv-Spalte für alle 21 Slots:** Motive 1:1 aus den `media.label`-Feldern des GrünWerk-Seeds (`lib/flagship/galabau/seed.ts`) abgeleitet — svc_01–05 matchen die 5 Leistungs-Karten (Gartenplanung/Pflaster/Pflege/Bewässerung/Zaun), why_1–3 die Warum-Karten, BA-Paar „fertiger Garten"/„Baustelle", Team-Rollen lt. Seed. Slot-Zählung gegen `asset-slots.ts` verifiziert (21 ✓).
- **Ketten-Logik ergänzt:** hero-video Motiv-Idee (Wind in Gräsern, Beleuchtung glimmt, Lichtstimmung konstant) + Hinweis, dass `BRANCHEN_VIDEO_STIL` keinen galabau-Eintrag hat (`DEFAULT_VIDEO_STIL` greift, Rezept-Stil im Prompt mitgeben).
- Asset-Lauf selbst bleibt ⛔ Credits-Freigabe (WARTELISTE „Higgsfield-Assets galabau generieren") — Rezept ist jetzt aber ohne Rückfrage ausführbar.

## Arbeitsblock: Scrub-PV Asset-Lauf — Higgsfield-Kette komplett, Scrub-Modus AKTIV

- **Freigabe:** Felix 2026-07-22 („gib alle credits frei, generiere die assets"). Verbrauch **232 von 246 Credits** (7 Entry Still gpt_image_2 2k + 5×45 seedance_2_0 5s/1080p/std/epic).
- **Seam-locked Kette exakt nach `rezepte/REZEPTE_SCRUB_PV.md`:** Entry Still → V1–V5 **sequentiell** (letzter Frame Clip N via `ffmpeg -ss 00:00:04.9` = start_image Clip N+1), QA je Clip vor dem nächsten (Seam-Vergleich, Bewegungsrichtung, Farbsprünge). Job-IDs: Entry afdc0b0e · V1 60b1c763 · V2 5c18a39b · V3 88805351 · V4 5bf5c464 · V5 c691e596. **V5-QA-Fall:** Endframe zeigt Wohnzimmer statt Speicher — Mittel-Frame-Extraktion (2.5s) belegt Speicher-Wandeinheit mit Cyan-LEDs im Clip ⇒ Motiv vorhanden, keine Neugenerierung nötig.
- **Frame-Budget-Pivot (≤5 MB):** Guide-Pfad (concat → jpg fps=24) ergab 34 MB; JPEG schafft das Budget nicht (beste Variante 5.24 MB bei miserabler Qualität). Lösung: **WebP via sharp** (lokales ffmpeg ohne libwebp) — hochwertige jpg-Quellframes (q:v 2) → sharp `.webp({quality:45, effort:6})`, fps 12, 540p ⇒ **303 Frames, 4,87 MB** ✓. Poster 1920×1080 via sharp mozjpeg q72 (145/125/61/62/87 KB).
- **Installiert `public/media/pv/`:** poster-01…05.jpg + frames/frame-0001…0303.webp (6,0 MB gesamt). `seed.ts`: alle 5 Szenen-Poster mit `datei`/`breite`/`hoehe`, `inhalte.frames` gesetzt (`/media/pv/frames/frame-NUM.webp`, gesamt 303, ziffern 4, fps 12, vorlade 20) ⇒ **Scrub-Modus aktiv**. Engine formatagnostisch (`pfad_muster`-Replace) — keine Engine-Änderung nötig.
- **`scripts/test-scrub.ts` → 140 Prüfungen:** Abschnitte 1/5b auf Klon-ohne-frames umgestellt (Modus-Mechanik-Intention erhalten); **neuer Abschnitt 6 „seed-assets"**: frames-Config, Seed rendert Canvas, erster/letzter Frame existiert, Frame gesamt+1 existiert NICHT, Dateianzahl === gesamt, Gesamtbytes ≤ 5 MB, alle 5 Poster-Dateien vorhanden — schützt dauerhaft gegen Config/Asset-Drift.
- **Screenshots `docs/screenshots/scrub/`:** [scrub-desktop-1440.png](docs/screenshots/scrub/scrub-desktop-1440.png) (Szene 1, Canvas aktiv: Abendhaus + Sonnen-Flare) · [scrub-scrolled-1440.png](docs/screenshots/scrub/scrub-scrolled-1440.png) (55 % Scroll = Szene 3, Cyan-Stromfluss über Modulen, Dots + Copy) · [scrub-mobile-390.png](docs/screenshots/scrub/scrub-mobile-390.png) — Sichtprüfung ✓ (Poster→Canvas-Übergang, Scroll-Scrub zeichnet korrekten Frame).
- Arbeitsdateien (Clips, Seams, Quellframes) in `.assets-work/` (gitignored, nicht committet).
- Selbsttest: `tsc --noEmit` ✅ · `test:scrub` **140** ✅ · `test:galabau` 130 ✅ · `test:maler` 204 ✅ · `test:flagship` 463 ✅ · Build ✅.

## Arbeitsblock: Premium Scroll-Scrub — Engine `scrub-story-v1` + PV-Seed (Solarflow)

- **Neue Premium-Komposition `lib/flagship/scrub/` (8 Dateien)** nach Felix' Guide „Scroll-Scrub PV Website" (Muster A4 „Seam-locked Scroll Scrub"): EINE kontinuierliche Kamerafahrt in 5 nahtlosen Szenen, Scrollen steuert die Zeit (Sonne → Modul → Stromfluss → Haus → Speicher). Dateien: `types.ts` (ScrubConfig/ScrubSzene/ScrubFrames + `istScrubKomposition`), `seed.ts` (PV-Seed „Solarflow"/Osnabrück/0541 000000 verbatim, Guide-Palette #07090c/#f5ff5a/#00e5ff, 5 Szenen mit Scroll-Gewichten 1.6/1.4/1.3/1.4/1.8), `asset-slots.ts` (10 Slots: poster_01–05 Pflicht 16:9 min 1920, clip_01–05 optional mit Poster-Fallback + `scrubSlotKeyAusDateiname`), `css.ts` (sticky Stage 100dvh, Szenen-Höhe via `--ss-gewicht`, prefers-reduced-motion → Poster), `js.ts` (Canvas-Frame-Scrubber: NUM-Padding, Lazy-Preload, cover-Math, Interpolation `+= diff*0.15`, DPR ≤2, Dots/Progress; iOS-sicher weil Canvas statt Video-Seeking), `sections.ts` (SSR-Copy immer im Markup, Formularvertrag `data-kontakt-form`), `render.ts` (Shell wie maler: JSON-LD LocalBusiness, `\u003c`-Escaping, noindex-Default), `copy-slots.ts` (Slot-Limits = Seed×1.15, Verbatim firma/ort/telefon).
- **Mechanischer Modus statt Stufen-Feld:** `inhalte.frames` gesetzt → Scrub-Modus (Canvas), fehlt → statischer Poster-Modus. Die Frame-Sequenz ist ein **Derivat der Clips** (ffmpeg fps=24) und wird bewusst NICHT slot-verwaltet — nur `inhalte.frames.pfad_muster` referenziert sie. Seed startet ohne frames ⇒ rendert automatisch statisch, bis echte Assets da sind.
- **Dispatch:** `lib/flagship/render.ts` — `istScrubKomposition` als erste Weiche vor maler/galabau; Union um `ScrubConfig` erweitert. galabau/maler unangetastet (Harte Regel 1).
- **Neu `scripts/test-scrub.ts` (`npm run test:scrub`): 123 Prüfungen grün** — Seed-Statisch (Marker, Verbatim, genau 1 h1, noindex-Default + `noindex:false`, Ribbon nur mit demo, submitZiel → `action="/api/anfrage"`), Scrub-Modus (Canvas/Poster/Loader/Dots/SSR-Copy, `--ss-gewicht:1.6`, Gewichte-JSON, XSS im `pfad_muster` → `\u003c`-escaped), Copy-Slots (≥30 Slots, zu_lang/fehlt, 3-Szenen ohne Phantom-Verstöße), Asset-Slots (10, Pflicht/Fallback, Dateinamen-Mapping), Stress (Langname escaped, Garmisch-Partenkirchen, `tel:+4908821123456` mechanisch). Lehre: 3 anfängliche False Positives durch Substring-Matches gegen Inline-CSS/JS — Assertions auf echtes Markup präzisiert.
- **Lighthouse (LHCI ad-hoc `.lighthouse/scrub/`, `noindex:false`):** statisch Perf 0.98 / SEO 1.0 / A11y 1.0 · scrub Perf 1.0 / SEO 1.0 / A11y 0.95 — Budget 90/95/90 erfüllt.
- **Screenshots `docs/screenshots/scrub/`:** [statisch-desktop-1440.png](docs/screenshots/scrub/statisch-desktop-1440.png) · [statisch-mobile-390.png](docs/screenshots/scrub/statisch-mobile-390.png) · [statisch-hero-1440x900.png](docs/screenshots/scrub/statisch-hero-1440x900.png) — Sichtprüfung Hero ok (dunkle Premium-Optik, gelbe CTA-Pill mit Glow, Szene-1-Copy).
- **Rezeptliste `rezepte/REZEPTE_SCRUB_PV.md`:** Entry Still (gpt_image_2, 7 Credits) + 5 seedance_2_0-Videos (225 Credits, **sequentiell**: letzter Frame N = start_image N+1), Seam-Locking/Encoding/Frame-Sequenz per ffmpeg, QA-Checklist, ~246 Credits gesamt ⇒ WARTELISTE (Freigabe Felix).
- Selbsttest: `tsc --noEmit` ✅ · `test:scrub` 123 ✅ · `test:galabau` 130 ✅ · `test:maler` 204 ✅ · `test:flagship` 463 ✅ · Lint nur Bestands-Warning · Build ✅. Kosten-Log: scrub 0 Cent (Stub-Phase).
- **Offen (Fragen an Felix, siehe WARTELISTE):** Premium-Preis/Stripe-Einordnung (4. Stufe über Growth vs. eigene Linie — `config/plans.ts` bewusst NICHT angefasst), PV als Branche 17 vs. Premium-Add-on, Higgsfield-Credit-Freigabe, Seed-Musterdaten, Demo-Platzierung, Kontakt-Placeholder „Kurz zu Ihrem Dach…" (PV-hartkodiert).

## Arbeitsblock: Maler B5 — Optik-Screenshots aller 3 Stufen (Template-Fabrik)

- **9 Playwright-Screenshots in `docs/screenshots/maler/`** (Stub-Assets, `reducedMotion:'reduce'` ⇒ Reveals sofort sichtbar + Wand im fertigen Salbei-Zustand — kein langsamer Scroll nötig):
  - Stufe statisch: [statisch-desktop.png](docs/screenshots/maler/statisch-desktop.png) · [statisch-mobile.png](docs/screenshots/maler/statisch-mobile.png) · [statisch-vp-hero.png](docs/screenshots/maler/statisch-vp-hero.png)
  - Stufe video: [video-desktop.png](docs/screenshots/maler/video-desktop.png) · [video-mobile.png](docs/screenshots/maler/video-mobile.png) · [video-vp-hero.png](docs/screenshots/maler/video-vp-hero.png)
  - Stufe growth: [growth-desktop.png](docs/screenshots/maler/growth-desktop.png) · [growth-mobile.png](docs/screenshots/maler/growth-mobile.png) · [growth-vp-hero.png](docs/screenshots/maler/growth-vp-hero.png)
- Desktop 1440 fullPage · Mobile 390 fullPage (@2x) · Hero-Viewport 1440×900; Quelle: `.lighthouse/maler/stufe-*.html` (identischer Render wie Lighthouse, `noindex:false`).
- Sichtprüfung: Signature-Wand fertig (Salbei, Tags ALTWEISS/SALBEI), Growth-Kontakt mit Rückruf-Checkbox (Datei-Anhang korrekt NICHT ohne submitZiel), Footer verbatim (Osnabrück · Belm · Georgsmarienhütte, 0541 000000). Fixed-Header-Stitching wie bei galabau harness-bedingt möglich, kein DOM-Bug.
- **✅ Optik-Freigabe Felix 2026-07-22** („gib alle frei") — `frozen:true` in `lib/flagship/maler/seed.ts` gesetzt; `maler-landing-v1` ist damit eingefroren (Änderungen ab jetzt nur mit neuer Freigabe).

## Arbeitsblock: Maler B4 — Stresstest + Lighthouse (Template-Fabrik)

- **Neu `scripts/test-maler.ts` (`npm run test:maler`):** 204 Prüfungen nach dem `test-galabau.ts`-Muster — Seed-Verbatim (Voss/Osnabrück/0541 000000), Stufen-Gates (statisch ohne Video/Galerie/Wand-JS · video mit `modus='einmal'` · growth mit wa.me/m-gebiet/Rückruf/Datei-Anhang nur mit submitZiel, m-reviews entfällt ohne echte Reviews), `signature_story:'on'`→scrub nur growth, alle 3 Unterseiten-Typen (Titles, H1 „{Leistung} in {Stadt}", Galerie-Filter-Buttons, unbekannter Pfad/Slug → null, basisPfad-Anker), Copy-Slot-Verstöße (zu_lang/fehlt), 27 Asset-Slots inkl. gal_01–06 + Dateinamen-Mapping, 3 fiese Datensätze (Langname GmbH & Co. KG escaped + JSON-LD, 3-Leistungen-Teilmenge → fehlende Unterseite null, Castrop-Rauxel verbatim), XSS-Escaping.
- **Bug gefunden + gefixt (`sections.ts`):** `malerWhatsappNummer('+49 541 …')` ergab `4949…` (Vorwahl doppelt) — jetzt Early-Return, wenn Ziffern bereits mit `49` beginnen.
- **Lighthouse (LHCI, Stub-Assets, `noindex:false` wie Live-Render):** alle 10 Seiten (3 Stufen + ueber-uns + referenzen + 5 `/leistungen/{slug}`) Perf 100 · A11y 92–95 · SEO 100 — Budget 90/95/90 überall erfüllt. Erster Lauf schlug nur wegen Demo-Default `noindex` fehl (`is-crawlable`), kein Template-Fehler.
- **QUALITY_CHECKLIST/Golden-Set:** Checkliste ist aus `lib/qa-gate/regeln.ts` generiert (keine Hand-Edits, keine Branchen-Zeilen); Golden-Set kennt wie bei galabau keinen Branchen-Eintrag — Präzedenz galabau: Matrix-Zelle dokumentiert Test-Anzahl + LH-Werte.
- Selbsttest: `tsc --noEmit` ✅ · `test:maler` 204 ✅ · `test:galabau` 130 ✅ · `test:flagship` 463 ✅ — galabau unangetastet.
- **Offen:** B5 Optik-Screenshots ⛔ Freigabe, `frozen:true` erst nach B5.

## Arbeitsblock: Maler B2 — Theme + Komposition `maler-landing-v1` (Template-Fabrik)

- **Theme `lib/flagship/themes/maler.ts`:** Salbei-Palette (#7BA88A Akzent, #F5F5F1 Hintergrund) als `KompositionTheme` — galabauCss ist theme-parametrisiert, keine galabau-Datei angefasst (Harte Regel 1).
- **Komposition `lib/flagship/maler/` (9 Dateien):** `types.ts` (MalerConfig/MalerInhalte extends Galabau + wand/galerie/module/leistung_details, `istMalerKomposition`), `seed.ts` (Voss Maler & Lackierer, Osnabrück — verbatim aus Steckbrief), `asset-slots.ts` (21 GaLaBau-Slots + gal_01–06 Galerie = 27, zod-Gate beim Modul-Load), `copy-slots.ts` (GaLaBau-Basis + Wand/Galerie/Gebiet/Leistungs-Details, Verbatim-Felder firma/ort/telefon), `sections.ts` (Re-Exports der GaLaBau-Sektionen + neu: m-wand, m-galerie mit Filter, m-gebiet, m-reviews, m-leistung-kopf, Kontakt-Growth-Extras Rückruf-Checkbox/Datei-Anhang, WhatsApp-Bubble mit mechanischer wa.me-Nummer), `css.ts` (galabauCss(MALER_THEME) + Extra-CSS), `js.ts` (galabauJs + Wand-IIFE), `render.ts` (Landing + Unterseiten).
- **Signature „Wand färbt sich":** nur `clip-path`/`--rolle` (0→1 aus Scroll-Position), Modi `einmal` (monotone Ratsche + Listener-Abbau) / `scrub` (nur growth + `signature_story:'on'`) / `fertig` (statisch, Unterseiten); CSS-Default `--rolle:1` ⇒ no-JS und `prefers-reduced-motion` zeigen die fertige Wand.
- **3 Render-Stufen (Zuordnung NUR in `config/plans.ts` — `renderStufeFuerTier`):** statisch = Onepager ohne Video/Galerie/Module · video = +Galerie +Wand-Animation (Demo-Default) · growth = +Module (WhatsApp, Rückruf, Datei-Anhang, Einzugsgebiet; Reviews nur echte — Seed bewusst ohne) +Unterseiten `/leistungen/{slug}` (H1 „{Leistung} in Osnabrück", eigene FAQ/CTA), `/ueber-uns`, `/referenzen` (Galerie mit Filter). Legacy `opts.level='growth'` ≡ `stufe:'growth'`.
- **Registrierung:** Dispatch in `lib/flagship/render.ts` (`istMalerKomposition` vor galabau), `SLOT_REGISTRY.maler` in `scripts/import-assets.ts`, `RenderStufe`/`renderStufeFuerTier` in `config/plans.ts`.
- **Smoke-Beweis:** statisch/video/growth 42/45/47 kB HTML; Stufen-Gates über Marker geprüft (statisch ohne m-galerie/wa.me/m-gebiet, video mit m-galerie ohne Growth-Module, growth mit wa.me/49541000000 + m-gebiet + Rückruf, m-reviews korrekt weggelassen); alle 5 Leistungs-Slugs matchen `leistung_details`; unbekannter Pfad → null (404); 0 Copy-Verstöße; Dispatch-Gleichheit `renderFlagshipPage === renderMalerLanding`.
- **Lint-Fix:** Variable `module` → `growthModule` (`@next/next/no-assign-module-variable`).
- Selbsttest: `tsc --noEmit` ✅ · `next build` ✅ · Lint ✅ (nur Bestands-Warnings) · `test:galabau` 130 ✅ · `test:flagship` 463 ✅ — galabau unangetastet.
- **Offen:** B3 Rezeptliste, B4 test-maler.ts + Lighthouse, B5 Optik-Screenshots ⛔ Freigabe, `frozen:true` erst nach B5.

## Arbeitsblock: Erste-Schritte-Dashboard (Get started, Kunden-Ansicht)

- **Neu `components/erste-schritte-dashboard.tsx`:** Standard-Ansicht für Kunden nach dem Livegang — große Live-Vorschau (iframe `/api/sites/{siteId}/preview`, Browser-Chrome mit Domain/Sitename) + Get-started-Checkliste mit Erledigt-Status: Webseite live ✓, Bilder hochladen, Eigene Domain verbinden (KEINE/AUSSTEHEND/AKTIV aus `domains`-Tabelle), Ersten Blog-Beitrag freigeben (Zähler FREIGEGEBEN/WARTET_AUF_FREIGABE aus `seo_landingpages`), Angaben vervollständigen (Wizard-Fortschritt). Jeder offene Schritt mit CTA auf die passende Unterseite.
- **`app/dashboard/[siteId]/page.tsx` Phase 4a:** `!isAdmin && siteIsLive && view !== 'editor'` → Erste-Schritte-Übersicht (Daten parallel via `Promise.all`); Editor weiterhin über `?view=editor` bzw. Button „Webseite bearbeiten". Admin- und Freigabe-Flows unverändert.
- **Funktions-Check Dashboard (auf Felix' Frage):** Kein externes „Builder UI Kit" — Editor ist Eigenbau (`components/site-editor.tsx`, Chat/Manuell/Verlauf + Live-iframe). Status: Editor ✅, Vorschau ✅, Bilder+KI ✅, SEO-Blog-Workflow ✅, Domain verbinden ⚠️ (Vercel-Token fehlt, WARTELISTE), Domain-Neuregistrierung ⚠️ (Registrar-Mock, WARTELISTE).
- Selbsttest: `tsc --noEmit` ✅ · `next build` ✅ · `lint` ✅ (nur vorbestehende Warnings in fremden Dateien).

## Arbeitsblock: LANDING-UMBAU A — Hero-Split (Marketing-Landing)

- **`components/landing/WvdClient.tsx` Hero umgebaut:** zentriertes Layout (`.hero-centered` + Video unten) → Links/Rechts-Split. Links: Badge-Pill „Professionelles Webdesign · Alles inklusive" → H1 mit festen Umbrüchen („Webseiten, die / Kunden bringen." + Akzent „Für unter 100 € / im Monat.", je max 2 Zeilen) → Sub (~34 Wörter, unverändert) → CTA-Zeile Primär „Kostenloses Erstgespräch buchen →" + Sekundär „Beispiele ansehen" (`#ergebnisse`) → 4 Trust-Pills (2×2 bei Platzmangel). Rechts: Video-Karte (`data-slot="hero_video_poster"`, Play-Button mit Akzent-Glow-Ring).
- **`app/(marketing)/marketing.css`:** `.hero-split` 12-Spalten-Grid ≥1024px (`align-items:center`, links Sp. 1–6, Video Sp. 7–12 über `grid-row: 1 / span 5`); Video-Karte weißes Liquid-Glass (Radius 26, Doppel-Schatten, 16:9, Float ±4px/6s nur `transform`); Karo-Maske auf `at 30% 20%`; alte Klassen `.hero-centered`/`.video-hero-container` entfernt (nirgends mehr referenziert). Mobil <1024: Flex-Column mit `order` → Badge→H1→Sub→CTA→Video→Trust, linksbündig, 16px Padding. `prefers-reduced-motion`: Float, Grid-Pulse, Text-Reveal und Fade-Ins aus, alles sofort sichtbar.
- **Abnahme A:** Screenshots `docs/screenshots/landing/hero-split-1440.png` + `hero-split-390.png` ✅ · H1-Unterkante 453px vs. Video-Mitte 468px (Delta 14px, eine Achse) ✅ · CLS 0.000 (PerformanceObserver, 1440) ✅ · nichts zentriert schwebend ✅.
- Video-Quelle noch nicht vorhanden — Karte zeigt Poster-Fläche (Slot `hero_video_poster`); Inline-Play wird verdrahtet, sobald das Video-Asset da ist (WARTELISTE).
- Bekanntes vorbestehendes Thema (nicht Teil dieses Umbaus): Nav-CTA läuft auf sehr schmalen Viewports (<400px) rechts an — separater Fix möglich.
- Selbsttest: `tsc --noEmit` ✅ · `next build` ✅ · `lint` ✅ (nur vorbestehende Warnings).

## Arbeitsblock: LANDING-UMBAU B — 5er-Bento mit DOM-Mini-Mockups (Marketing-Landing)

- **`components/landing/WvdClient.tsx` Features-Sektion umgebaut:** alte `.bento-grid` (5 Icon-Karten „24/7 Online" etc.) → `.features-bento` mit 5 Zellen und Mini-Mockups komplett aus DOM+CSS (keine Bilder, keine Icon-Fonts als Hauptmotiv). Section-Head „Was du bekommst / Alles inklusive. Ohne Kompromisse." unverändert.
  1. **„In Tagen live"** (große Zelle links): Browser-Mockup mit Ampel-Punkten, URL-Pill `malermeister-krause.de` mit Schloss, grüner Live-Badge (Punkt pulsiert 1×), Mini-Website (Farbflächen-Hero + 2 Karten). Hero-Fläche wechselt alle 3 s durch 3 Branchen-Farben (#3FA463 GaLaBau → #7BA88A Maler → #2E6FA8 SHK, `fbBrancheCycle` 9 s).
  2. **„Sie schreiben, die KI baut":** Chat-Szene — Nutzer-Bubble „Mach die Überschrift größer", Antwort-Karte „Malerbetrieb Krause" (Headline morpht beim Hover `scale(1.18)`, einmalig), Pill „✓ Veröffentlicht".
  3. **„Bei Google gefunden":** Such-Pill mit Lupe „Maler Wiesbaden", EIN Snippet (Favicon-Punkt, blaue Titelzeile, grüne URL-Zeile), Pill „Platz 1" — keine Google-Logos.
  4. **„Anfragen direkt aufs Handy":** 3 gestapelte Notification-Karten; oberste mit Avatar „S", „Neue Anfrage · vor 2 Min", Anfrage-Text, Akzent-Pfeil; untere 2 versetzt + geblurrt; Hover fächert um 4 px.
  5. **„Alles inklusive":** Checkliste Design/Hosting/SEO/Support ✓, Preis-Pill „99 €/Monat" mit Glow-Ring, „DSGVO"-Chip.
- **`app/(marketing)/marketing.css`:** `.features-bento` als `grid-template-areas` („web web editor editor google google" / „web web anfragen anfragen inklusive inklusive") — 1 große links, 4 gleiche rechts 2×2; ≤1024px 2-spaltig (web volle Breite), ≤768px einspaltig (große zuerst). Zellen `--surface-card`, Radius 26, Doppel-Schatten, Hover `translateY(-3px)`; Illustrationsfläche `--gray-050` mit 4er-Karo (`background-size: 22px`, ohne Maske), Mikro-Texte 10–12px, je Zelle EIN bewegtes Element. Alte Klassen `.bento-grid`/`.bento-card`/`.card-visual`/`.bento-icon-glow` inkl. Media-Query-Referenzen entfernt (`.bento-section`/`.beams` bleiben — werden vom Rechner mitgenutzt). `prefers-reduced-motion`: Branchen-Cycle, Live-Pulse und Hover-Transitions aus.
- **Abnahme B:** Screenshots `docs/screenshots/landing/bento-1440.png` + `bento-390.png` ✅ · 1440: große Zelle 392×643 links, 4 gleiche Zellen 392px rechts 2×2 ✅ · 390: einspaltig, große zuerst ✅ · Mockups DOM-only ✅ · max 3 Ebenen je Mockup ✅.
- Selbsttest: `tsc --noEmit` ✅ · `next build` ✅ · `lint` ✅ (nur vorbestehende Warnings).

## Arbeitsblock: T2 — Asset-Import-Script (GaLaBau → Template-Fabrik)

- **`scripts/import-assets.ts` neu:** `npx tsx scripts/import-assets.ts --branche galabau --dir <ordner> [--approve] [--stub]`. Zwei Phasen:
  - `planeImport()` (pur, ohne DB): Dateiname→Slot per `slotKeyAusDateiname` (Vertrag rezepte/REZEPTE_GALABAU.md, unbekannte Namen werden abgelehnt — kein Raten), Duplikat-Slots abgelehnt, sharp-Validierung (min_width, Aspect ±5 % wie lib/assets/aspekt.ts), `ba_before`+`ba_after` bekommen automatisch dieselbe `pair_id` (nur wenn BEIDE da, halbe Paare → Warnung), fehlende Pflicht-Slots werden gemeldet.
  - `fuehreImportAus()` (DB): WebP(82)+AVIF(55) in srcset-Stufen 480/960/1600 (wie lib/assets/pipeline.ts), Upload nach Bucket `asset-bank` unter `{branche}/{yyyy-mm}/{assetId}/{breite}.{format}`, `asset_bank`-Insert mit style_tags aus `stilFuerBranche`, `quality_status` draft (oder approved bei `--approve`). Video: ≤3 MB (config/video-guidelines.ts), größere per ffmpeg transkodiert (1280px, ohne Ton), Poster = hero_bg aus demselben Import oder approved Hero aus der Bank; Varianten-Shape wie Admin-Video-Route (`video_url`/`poster_url`/`groesse_bytes`).
  - `--stub`: quelle='ai_mock' → NIE approvebar (Regel aus /admin/assets), `--approve` wird erzwungen ignoriert. Ohne Stub: quelle='demo_generiert'.
  - `SLOT_REGISTRY` (Config over Hardcode): neue Branchen tragen Slots + Dateinamen-Mapping ein (Fabrik B2).
- **Selbsttest `scripts/test-import-assets.ts` (STUB-Fallback laut Auftrag):** 13/13 ✅ — 3 sharp-generierte Stub-Bilder (hero-bg 1920×1080, ba-after/ba-before 1600×1200) korrekt geplant inkl. gemeinsamer pair_id; Ablehnungen geprüft (unbekannter Name `foo.jpg`, zu klein `svc-01.jpg` 800px, falscher Aspect `about-detail.jpg` 16:9 statt 4:3); fehlendePflicht = 13 Slots (hero_video + Avatare optional) ✅; unbekannte Branche wirft mit Registry-Hinweis ✅; halbes Paar → Warnung + ohne pair_id ✅. DB-Phase (STUB-Insert + Cleanup) im Test enthalten, aktuell übersprungen: `SUPABASE_SERVICE_ROLE_KEY` fehlt lokal (WARTELISTE).
- **DoD T2 (≥25 approved Assets inkl. Paar/Hero/Video) offen:** braucht echte Higgsfield-Assets + Service-Role-Key + ausgeführte Migration 022 → WARTELISTE. Script + Vertrag stehen; Import ist dann ein Einzeiler.
- Selbsttest: `tsc --noEmit` ✅ · `next build` ✅ · `lint` ✅ (nur vorbestehende Warnings).

## Notizen für nächste Session
- **MVP-Finish Phasen 0–6 abgeschlossen (inkl. Bausteine A/B/C).** Rest = Mensch/Key-Punkte auf WARTELISTE.md (§8-Gesamtabnahme braucht Stripe-Test-Keys, Vercel-Envs, Asset-/Branchen-Freigaben, Git-Remote für CI)
- **Alle Phasen 0–H abgeschlossen.** Offen ist nur noch, was ein Mensch liefern muss → WARTELISTE.md (Migrationen 013–021 ausführen, Library seeden, Stripe-Test-/Webhook-Keys, Git-Remote + erster Push für CI, E2E-Freischaltung, echte Golden-Set-Firmen, Preis-Bestätigungen)
- Gesamt-DoD „kompletter Testmode-Durchlauf ohne manuellen Eingriff": als env-gated E2E implementiert (`E2E_ENABLED=1 npm run test:e2e`); tatsächlicher Grün-Lauf blockiert allein durch fehlende Stripe-Testkeys + nicht migrierte Supabase (WARTELISTE „E2E-Lauf freischalten")
- Plan-Upgrade-Proration: weiterhin pragmatisch (neue Subscription + manual_task) — Verbesserung mit `subscription.update` + Proration möglich, bewusst nicht in H gebaut
- Ads-Preis (99 €/M) und alle Upsell-Preise sind Platzhalter → Bestätigung auf WARTELISTE; echte Ads-Leistungsdaten erst mit Google-Ads-API-Zugang
- Migrationen 013–021 noch nicht in Supabase ausgeführt (WARTELISTE); danach: `npx tsx scripts/seed-library.ts`
- Library-Demos: DB-Spalte `engine` wird erst nach Migration 017 geschrieben — der öffentliche Renderer hängt NICHT daran (brancht auf `config.engine`), aber neue Library-Demos brauchen Migration 017 (Insert setzt engine='library')
- Stock-Assets sind Platzhalter (Friseur nutzt universelle Bilder) — echte lizenzierte Assets auf WARTELISTE
- Firecrawl/Places-Keys optional (WARTELISTE) — Kette degradiert sanft auf eigenen Scraper + manuelle Daten
